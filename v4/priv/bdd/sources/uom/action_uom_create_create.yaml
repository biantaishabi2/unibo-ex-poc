module: UOM_UOM
contract: Uom.create action contract
edge_class: action_contract
source_file: models/uom/uom.ubo.yaml
source_summary: GIVEN 实体 Uom 的动作 create 可执行，accept=[name, factor, rounding, uom_type, active]，args=[category_id:uuid!] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create 创建计量单位 -> update 修改换算比率/精度
