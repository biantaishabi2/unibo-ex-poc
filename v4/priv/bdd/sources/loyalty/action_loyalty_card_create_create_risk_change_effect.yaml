module: LOYALTY_LOYALTY_CARD
contract: LoyaltyCard.create action contract (risk:change_effect)
edge_class: action_contract
source_file: models/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 LoyaltyCard 的动作 create 触发副作用 / WHEN 执行动作 create / THEN 变更副作用应完整生效且状态一致 / 补充场景：create -> earn_points(100) -> earn_points(50) -> redeem_points(80)；create -> freeze -> unfreeze -> expire_card
