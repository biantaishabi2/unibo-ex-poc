module: LOYALTY_COUPON
contract: Coupon.apply action contract (risk:success)
edge_class: action_contract
source_file: models/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 Coupon 的动作 apply 可执行 / WHEN 执行动作 apply / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create(public, percent, 10%) -> apply(order_1) -> apply(order_2)；create(one_time) -> apply -> apply 第二次应报错；create -> cancel_coupon -> apply 应被拒绝
