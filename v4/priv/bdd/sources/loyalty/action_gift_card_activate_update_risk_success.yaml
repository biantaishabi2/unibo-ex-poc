module: LOYALTY_GIFT_CARD
contract: GiftCard.activate action contract (risk:success)
edge_class: action_contract
source_file: models/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 GiftCard 的动作 activate 可执行 / WHEN 执行动作 activate / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create -> activate -> topup(200) -> charge(150) -> charge(50) -> status=depleted；create(reloadable=false) -> activate -> topup 应报错；create -> activate -> cancel_card
