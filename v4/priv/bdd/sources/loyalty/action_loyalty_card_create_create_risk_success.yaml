module: LOYALTY_LOYALTY_CARD
contract: LoyaltyCard.create action contract (risk:success)
edge_class: action_contract
source_file: models/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 LoyaltyCard 的动作 create 可执行 / WHEN 执行动作 create / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create -> earn_points(100) -> earn_points(50) -> redeem_points(80)；create -> freeze -> unfreeze -> expire_card
