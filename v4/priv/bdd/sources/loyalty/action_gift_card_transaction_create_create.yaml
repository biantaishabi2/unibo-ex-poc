module: LOYALTY_GIFT_CARD_TRANSACTION
contract: GiftCardTransaction.create action contract
edge_class: action_contract
source_file: models/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 GiftCardTransaction 的动作 create 可执行，accept=[transaction_type, amount, balance_after, reference_id, note]，args=[gift_card_id:uuid!, operator_id:uuid] / WHEN 执行动作 create / THEN 状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create(topup) -> create(charge) -> create(refund)
