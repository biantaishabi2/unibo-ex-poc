module: LOYALTY_LOYALTY_CARD
contract: LoyaltyCard.expire_card action contract
edge_class: action_contract
source_file: models/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 LoyaltyCard 的动作 expire_card 可执行，accept=[expiry_date]，args=[无显式参数] / WHEN 执行动作 expire_card / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束
