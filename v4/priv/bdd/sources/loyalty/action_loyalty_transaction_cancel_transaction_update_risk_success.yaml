module: LOYALTY_LOYALTY_TRANSACTION
contract: LoyaltyTransaction.cancel_transaction action contract (risk:success)
edge_class: action_contract
source_file: models/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 LoyaltyTransaction 的动作 cancel_transaction 可执行 / WHEN 执行动作 cancel_transaction / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create(earned, +200) -> cancel_transaction
