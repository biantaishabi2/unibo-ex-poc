module: LOYALTY_LOYALTY_TRANSACTION
contract: LoyaltyTransaction.cancel_transaction action contract (risk:change_effect)
edge_class: action_contract
source_file: models/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 LoyaltyTransaction 的动作 cancel_transaction 触发副作用 / WHEN 执行动作 cancel_transaction / THEN 变更副作用应完整生效且状态一致 / 补充场景：create(earned, +200) -> cancel_transaction
