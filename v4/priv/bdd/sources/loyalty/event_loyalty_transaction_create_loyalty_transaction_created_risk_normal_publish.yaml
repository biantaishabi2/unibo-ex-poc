module: LOYALTY_LOYALTY_TRANSACTION
contract: LoyaltyTransaction.create event contract (risk:normal_publish)
edge_class: event_contract
source_file: models/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 LoyaltyTransaction 动作 create 触发事件 / WHEN 发布 topic `loyalty.transaction.created` / THEN 消费方应收到一次有效通知
