module: LOYALTY_LOYALTY_CARD
contract: LoyaltyCard.redeem_points action contract
edge_class: action_contract
source_file: models/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 LoyaltyCard 的动作 redeem_points 可执行，accept=[points_balance]，args=[points_delta:decimal!, reward_id:uuid] / WHEN 执行动作 redeem_points / THEN 输入应满足校验规则，状态变更与副作用应生效，应触发关联事件，应符合工作流步骤约束
