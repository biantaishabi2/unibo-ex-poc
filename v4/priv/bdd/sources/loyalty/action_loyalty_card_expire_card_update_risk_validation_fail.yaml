module: LOYALTY_LOYALTY_CARD
contract: LoyaltyCard.expire_card action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 LoyaltyCard 的动作 expire_card 输入非法 / WHEN 执行动作 expire_card / THEN 应返回校验失败且不产生持久化副作用
