module: LOYALTY_LOYALTY_TRANSACTION
contract: LoyaltyTransaction.create action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 LoyaltyTransaction 的动作 create 输入非法 / WHEN 执行动作 create / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create(earned, +100) -> create(redeemed, -50) -> create(expired, -50)
