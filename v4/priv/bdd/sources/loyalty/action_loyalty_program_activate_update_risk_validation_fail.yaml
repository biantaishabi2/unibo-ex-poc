module: LOYALTY_LOYALTY_PROGRAM
contract: LoyaltyProgram.activate action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 LoyaltyProgram 的动作 activate 输入非法 / WHEN 执行动作 activate / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create -> activate -> pause -> activate -> expire；create -> update(points_per_currency) -> activate
