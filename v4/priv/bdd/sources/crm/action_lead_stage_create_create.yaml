module: CRM_LEAD_STAGE
contract: LeadStage.create action contract
edge_class: action_contract
source_file: models/crm/crm.ubo.yaml
source_summary: GIVEN 实体 LeadStage 的动作 create 可执行，accept=[name, sequence, is_won, fold, probability, requirements, description]，args=[team_id:uuid] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效
