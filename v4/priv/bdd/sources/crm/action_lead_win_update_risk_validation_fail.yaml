module: CRM_LEAD
contract: Lead.win action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/crm/crm.ubo.yaml
source_summary: GIVEN 实体 Lead 的动作 win 输入非法 / WHEN 执行动作 win / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：active=false 时尝试 win 被校验拒绝
