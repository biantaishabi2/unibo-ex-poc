module: SPREADSHEET_GLOBAL_FILTER
contract: GlobalFilter.apply_filter action contract (risk:success)
edge_class: action_contract
source_file: models/spreadsheet/spreadsheet.ubo.yaml
source_summary: GIVEN 实体 GlobalFilter 的动作 apply_filter 可执行 / WHEN 执行动作 apply_filter / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create -> update -> apply_filter -> destroy
