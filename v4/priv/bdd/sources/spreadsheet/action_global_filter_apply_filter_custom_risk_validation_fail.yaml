module: SPREADSHEET_GLOBAL_FILTER
contract: GlobalFilter.apply_filter action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/spreadsheet/spreadsheet.ubo.yaml
source_summary: GIVEN 实体 GlobalFilter 的动作 apply_filter 输入非法 / WHEN 执行动作 apply_filter / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create -> update -> apply_filter -> destroy
