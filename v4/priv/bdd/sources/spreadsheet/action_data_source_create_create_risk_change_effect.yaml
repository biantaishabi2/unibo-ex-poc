module: SPREADSHEET_DATA_SOURCE
contract: DataSource.create action contract (risk:change_effect)
edge_class: action_contract
source_file: models/spreadsheet/spreadsheet.ubo.yaml
source_summary: GIVEN 实体 DataSource 的动作 create 触发副作用 / WHEN 执行动作 create / THEN 变更副作用应完整生效且状态一致 / 补充场景：create -> update -> refresh -> destroy
