module: SPREADSHEET_GLOBAL_FILTER
contract: GlobalFilter.apply_filter action contract (risk:change_effect)
edge_class: action_contract
source_file: models/spreadsheet/spreadsheet.ubo.yaml
source_summary: GIVEN 实体 GlobalFilter 的动作 apply_filter 触发副作用 / WHEN 执行动作 apply_filter / THEN 变更副作用应完整生效且状态一致 / 补充场景：create -> update -> apply_filter -> destroy
