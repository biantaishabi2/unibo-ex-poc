module: RATING_RATING
contract: Rating.submit action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/rating/rating.ubo.yaml
source_summary: GIVEN 实体 Rating 的动作 submit 输入非法 / WHEN 执行动作 submit / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：submit 重复提交同一资源评价 -> 拒绝，返回 already_rated
