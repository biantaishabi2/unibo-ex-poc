module: EXPENSES_EXPENSE_LINE
contract: ExpenseLine.split action contract
edge_class: action_contract
source_file: models/expenses/expenses.ubo.yaml
source_summary: GIVEN 实体 ExpenseLine 的动作 split 可执行，accept=[(未声明)]，args=[无显式参数] / WHEN 执行动作 split / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束
