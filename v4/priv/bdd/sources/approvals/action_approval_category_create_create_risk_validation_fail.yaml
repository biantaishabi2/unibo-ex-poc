module: APPROVALS_APPROVAL_CATEGORY
contract: ApprovalCategory.create action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/approvals/approvals.ubo.yaml
source_summary: GIVEN 实体 ApprovalCategory 的动作 create 输入非法 / WHEN 执行动作 create / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create 审批类型，update 修改审批规则，deactivate 停用
