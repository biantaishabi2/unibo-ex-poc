module: DELIVERY_SHIPMENT_PACKAGE_CONTENT
contract: ShipmentPackageContent.shipment_package_content_flow workflow contract (risk:mainline)
edge_class: workflow_contract
source_file: models/delivery/delivery.ubo.yaml
source_summary: GIVEN 实体 ShipmentPackageContent 定义工作流 shipment_package_content_flow / WHEN 按主路径执行 create -> update -> destroy / THEN 主流程应可达并保持顺序一致
