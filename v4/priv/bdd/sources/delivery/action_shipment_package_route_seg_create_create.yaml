module: DELIVERY_SHIPMENT_PACKAGE_ROUTE_SEG
contract: ShipmentPackageRouteSeg.create action contract
edge_class: action_contract
source_file: models/delivery/delivery.ubo.yaml
source_summary: GIVEN 实体 ShipmentPackageRouteSeg 的动作 create 可执行，accept=[shipment_id, shipment_package_seq_id, shipment_route_segment_id, tracking_code]，args=[无显式参数] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create 创建包裹路段 -> update_label 更新面单 -> mark_printed 标记打印 -> update_cost 录入费用
