module: CALENDAR_CALENDAR_EVENT
contract: CalendarEvent.mark_tentative action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/calendar/calendar.ubo.yaml
source_summary: GIVEN 实体 CalendarEvent 的动作 mark_tentative 输入非法 / WHEN 执行动作 mark_tentative / THEN 应返回校验失败且不产生持久化副作用
