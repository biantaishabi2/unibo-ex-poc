module: CALENDAR_CALENDAR_EVENT
contract: CalendarEvent.calendar_event_lifecycle_flow workflow contract (risk:mainline)
edge_class: workflow_contract
source_file: models/calendar/calendar.ubo.yaml
source_summary: GIVEN 实体 CalendarEvent 定义工作流 calendar_event_lifecycle_flow / WHEN 按主路径执行 create -> update -> confirm -> cancel -> revert_to_draft -> mark_tentative -> destroy / THEN 主流程应可达并保持顺序一致
