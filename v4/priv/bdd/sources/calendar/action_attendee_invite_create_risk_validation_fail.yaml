module: CALENDAR_ATTENDEE
contract: Attendee.invite action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/calendar/calendar.ubo.yaml
source_summary: GIVEN 实体 Attendee 的动作 invite 输入非法 / WHEN 执行动作 invite / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：invite 邀请参与者 -> update_response 接受邀请 -> remove 移除参与者；invite 邀请参与者 -> update_response 拒绝邀请
