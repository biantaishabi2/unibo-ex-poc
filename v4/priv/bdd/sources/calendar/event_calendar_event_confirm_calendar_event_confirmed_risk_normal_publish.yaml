module: CALENDAR_CALENDAR_EVENT
contract: CalendarEvent.confirm event contract (risk:normal_publish)
edge_class: event_contract
source_file: models/calendar/calendar.ubo.yaml
source_summary: GIVEN 实体 CalendarEvent 动作 confirm 触发事件 / WHEN 发布 topic `calendar.event.confirmed` / THEN 消费方应收到一次有效通知
