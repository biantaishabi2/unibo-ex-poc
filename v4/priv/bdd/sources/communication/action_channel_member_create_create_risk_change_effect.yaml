module: COMMUNICATION_CHANNEL_MEMBER
contract: ChannelMember.create action contract (risk:change_effect)
edge_class: action_contract
source_file: models/communication/communication.ubo.yaml
source_summary: GIVEN 实体 ChannelMember 的动作 create 触发副作用 / WHEN 执行动作 create / THEN 变更副作用应完整生效且状态一致 / 补充场景：create -> update -> mark_fetched -> mark_seen -> destroy；成员变更触发频道级别的广播和通知；seen_message_id / fetched_message_id 引用 Message 记录；成员状态变更（join/leave/typing/seen）广播；_rtc_join_call() 创建会话，SFU 超过 3 人阈值自动启用；custom_notifications 和 mute_until_dt 影响通知分发
