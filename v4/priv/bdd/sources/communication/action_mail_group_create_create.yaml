module: COMMUNICATION_MAIL_GROUP
contract: MailGroup.create action contract
edge_class: action_contract
source_file: models/communication/communication.ubo.yaml
source_summary: GIVEN 实体 MailGroup 的动作 create 可执行，accept=[name, description, active, moderation, access_mode, moderation_notify, moderation_notify_msg, moderation_guidelines, moderation_guidelines_msg, image_128]，args=[无显式参数] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create -> join -> leave -> update -> destroy
