module: MARKETING_EVENT
contract: Event.create action contract
edge_class: action_contract
source_file: models/marketing/marketing.ubo.yaml
source_summary: GIVEN 实体 Event 的动作 create 可执行，accept=[event_code, name, start_date, end_date, date_tz, location, seats_max, seats_limited, auto_confirm, description, event_type_id]，args=[campaign_id:uuid] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应触发关联事件，应符合工作流步骤约束 / 补充场景：create 创建活动 -> publish 发布活动 -> set_done 标记完成
