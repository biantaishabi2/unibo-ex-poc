module: MARKETING_MAILING_LIST_MEMBER
contract: MailingListMember.create action contract (risk:success)
edge_class: action_contract
source_file: models/marketing/marketing.ubo.yaml
source_summary: GIVEN 实体 MailingListMember 的动作 create 可执行 / WHEN 执行动作 create / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 添加成员 -> unsubscribe 取消订阅 -> subscribe 重新订阅
