module: MARKETING_SOCIAL_POST
contract: SocialPost.create action contract (risk:change_effect)
edge_class: action_contract
source_file: models/marketing/marketing.ubo.yaml
source_summary: GIVEN 实体 SocialPost 的动作 create 触发副作用 / WHEN 执行动作 create / THEN 变更副作用应完整生效且状态一致 / 补充场景：create 创建帖子 -> update 编辑内容 -> publish_now 立即发布 -> sync_stats 同步统计；create 创建帖子 -> schedule 定时发布 -> sync_stats 同步统计
