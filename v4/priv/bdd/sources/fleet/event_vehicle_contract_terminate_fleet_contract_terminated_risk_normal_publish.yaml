module: FLEET_VEHICLE_CONTRACT
contract: VehicleContract.terminate event contract (risk:normal_publish)
edge_class: event_contract
source_file: models/fleet/fleet.ubo.yaml
source_summary: GIVEN 实体 VehicleContract 动作 terminate 触发事件 / WHEN 发布 topic `fleet.contract.terminated` / THEN 消费方应收到一次有效通知
