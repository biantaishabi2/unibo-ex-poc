module: FLEET_VEHICLE_CONTRACT
contract: VehicleContract.activate action contract (risk:change_effect)
edge_class: action_contract
source_file: models/fleet/fleet.ubo.yaml
source_summary: GIVEN 实体 VehicleContract 的动作 activate 触发副作用 / WHEN 执行动作 activate / THEN 变更副作用应完整生效且状态一致 / 补充场景：create 创建合同 -> activate 激活合同 -> renew 续签合同；create 创建合同 -> activate 激活合同 -> terminate 终止合同
