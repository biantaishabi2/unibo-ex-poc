module: FLEET_VEHICLE_SERVICE
contract: VehicleService.cancel action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/fleet/fleet.ubo.yaml
source_summary: GIVEN 实体 VehicleService 的动作 cancel 输入非法 / WHEN 执行动作 cancel / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：schedule 预约服务 -> cancel 取消服务
