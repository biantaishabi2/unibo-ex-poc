module: HR_WORK_ENTRY
contract: WorkEntry.create action contract
edge_class: action_contract
source_file: models/hr/hr.ubo.yaml
source_summary: GIVEN 实体 WorkEntry 的动作 create 可执行，accept=[name, date_start, date_stop, duration]，args=[employee_id:uuid!, work_entry_type_id:uuid] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应触发关联事件，应符合工作流步骤约束
