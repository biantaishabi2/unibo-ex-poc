module: HR_LEAVE_REQUEST
contract: LeaveRequest.create action contract
edge_class: action_contract
source_file: models/hr/hr.ubo.yaml
source_summary: GIVEN 实体 LeaveRequest 的动作 create 可执行，accept=[date_from, date_to, reason]，args=[employee_id:uuid!, leave_type_id:uuid!] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束
