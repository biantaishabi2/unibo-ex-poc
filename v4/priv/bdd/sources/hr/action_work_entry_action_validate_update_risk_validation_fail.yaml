module: HR_WORK_ENTRY
contract: WorkEntry.action_validate action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/hr/hr.ubo.yaml
source_summary: GIVEN 实体 WorkEntry 的动作 action_validate 输入非法 / WHEN 执行动作 action_validate / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create 后 action_validate，工时条目进入 validated
