module: PAYMENT_PAYMENT_TOKEN
contract: PaymentToken.create action contract (risk:success)
edge_class: action_contract
source_file: models/payment/payment.ubo.yaml
source_summary: GIVEN 实体 PaymentToken 的动作 create 可执行 / WHEN 执行动作 create / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 创建令牌 -> update 设为默认 -> revoke 吊销；create -> expire 标记过期
