module: PAYMENT_PAYMENT
contract: Payment.cancel action contract
edge_class: action_contract
source_file: models/payment/payment.ubo.yaml
source_summary: GIVEN 实体 Payment 的动作 cancel 可执行，accept=[comments]，args=[无显式参数] / WHEN 执行动作 cancel / THEN 输入应满足校验规则，状态变更与副作用应生效，应触发关联事件，应符合工作流步骤约束 / 补充场景：create -> submit -> cancel 取消支付(cancelled)
