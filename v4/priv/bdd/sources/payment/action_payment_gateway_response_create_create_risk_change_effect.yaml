module: PAYMENT_PAYMENT_GATEWAY_RESPONSE
contract: PaymentGatewayResponse.create action contract (risk:change_effect)
edge_class: action_contract
source_file: models/payment/payment.ubo.yaml
source_summary: GIVEN 实体 PaymentGatewayResponse 的动作 create 触发副作用 / WHEN 执行动作 create / THEN 变更副作用应完整生效且状态一致 / 补充场景：create 记录授权响应（gateway_code=00，result_declined=false）；create 记录拒绝响应（result_declined=true）；create 记录退款响应（payment_service_type_enum_id=refund）
