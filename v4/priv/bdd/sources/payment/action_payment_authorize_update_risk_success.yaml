module: PAYMENT_PAYMENT
contract: Payment.authorize action contract (risk:success)
edge_class: action_contract
source_file: models/payment/payment.ubo.yaml
source_summary: GIVEN 实体 Payment 的动作 authorize 可执行 / WHEN 执行动作 authorize / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 创建支付(draft) -> submit 提交(pending) -> authorize 授权(authorized) -> capture 捕获(captured)；create -> submit -> authorize -> capture -> refund 退款(refunded)；create -> submit -> authorize -> mark_failed 标记失败(failed)
