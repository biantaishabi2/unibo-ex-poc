module: ECOMMERCE_LOYALTY_RULE
contract: LoyaltyRule.update action contract
edge_class: action_contract
source_file: models/ecommerce/ecommerce.ubo.yaml
source_summary: GIVEN 实体 LoyaltyRule 的动作 update 可执行，accept=[code, minimum_qty, minimum_amount, reward_point_amount, reward_point_mode]，args=[无显式参数] / WHEN 执行动作 update / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束
