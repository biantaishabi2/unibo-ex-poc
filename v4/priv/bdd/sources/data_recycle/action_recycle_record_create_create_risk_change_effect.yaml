module: DATA_RECYCLE_RECYCLE_RECORD
contract: RecycleRecord.create action contract (risk:change_effect)
edge_class: action_contract
source_file: models/data_recycle/data_recycle.ubo.yaml
source_summary: GIVEN 实体 RecycleRecord 的动作 create 触发副作用 / WHEN 执行动作 create / THEN 变更副作用应完整生效且状态一致 / 补充场景：create 新建待回收记录，validate 执行确认回收；create 新建待回收记录，discard 忽略记录并设为非活跃
