module: DATA_RECYCLE_RECYCLE_MODEL
contract: RecycleModel.create action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/data_recycle/data_recycle.ubo.yaml
source_summary: GIVEN 实体 RecycleModel 的动作 create 输入非法 / WHEN 执行动作 create / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create 新建回收规则，update 调整时间窗口与回收模式；create 新建回收规则后，recycle_records_action 手动触发回收任务；create 新建自动规则后，cron_recycle 定时执行批量扫描与回收；create 新建回收规则后，destroy 删除规则；create 时 recycle_action 非法值，校验失败
