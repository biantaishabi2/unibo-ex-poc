module: DATA_RECYCLE_RECYCLE_RECORD
contract: RecycleRecord.discard action contract (risk:success)
edge_class: action_contract
source_file: models/data_recycle/data_recycle.ubo.yaml
source_summary: GIVEN 实体 RecycleRecord 的动作 discard 可执行 / WHEN 执行动作 discard / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：discard 后再次 validate，因 active=false 校验失败
