module: DATA_RECYCLE_RECYCLE_RECORD
contract: RecycleRecord.discard action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/data_recycle/data_recycle.ubo.yaml
source_summary: GIVEN 实体 RecycleRecord 的动作 discard 输入非法 / WHEN 执行动作 discard / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：discard 后再次 validate，因 active=false 校验失败
