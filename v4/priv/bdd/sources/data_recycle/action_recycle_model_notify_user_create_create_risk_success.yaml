module: DATA_RECYCLE_RECYCLE_MODEL_NOTIFY_USER
contract: RecycleModelNotifyUser.create action contract (risk:success)
edge_class: action_contract
source_file: models/data_recycle/data_recycle.ubo.yaml
source_summary: GIVEN 实体 RecycleModelNotifyUser 的动作 create 可执行 / WHEN 执行动作 create / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 新增回收规则通知用户关联，destroy 删除关联；create 缺少 recycle_model_id 或 user_id，校验失败
