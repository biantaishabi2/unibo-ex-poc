module: DATA_RECYCLE_RECYCLE_MODEL_NOTIFY_USER
contract: RecycleModelNotifyUser.create action contract
edge_class: action_contract
source_file: models/data_recycle/data_recycle.ubo.yaml
source_summary: GIVEN 实体 RecycleModelNotifyUser 的动作 create 可执行，accept=[[]]，args=[recycle_model_id:uuid!, user_id:uuid!] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应触发关联事件，应符合工作流步骤约束 / 补充场景：create 新增回收规则通知用户关联，destroy 删除关联；create 缺少 recycle_model_id 或 user_id，校验失败
