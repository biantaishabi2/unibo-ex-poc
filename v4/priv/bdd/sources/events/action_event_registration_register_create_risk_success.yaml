module: EVENTS_EVENT_REGISTRATION
contract: EventRegistration.register action contract (risk:success)
edge_class: action_contract
source_file: models/events/events.ubo.yaml
source_summary: GIVEN 实体 EventRegistration 的动作 register 可执行 / WHEN 执行动作 register / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：register 容量已满时返回 capacity_exceeded
