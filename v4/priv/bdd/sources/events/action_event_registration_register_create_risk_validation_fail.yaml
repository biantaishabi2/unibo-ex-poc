module: EVENTS_EVENT_REGISTRATION
contract: EventRegistration.register action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/events/events.ubo.yaml
source_summary: GIVEN 实体 EventRegistration 的动作 register 输入非法 / WHEN 执行动作 register / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：register 容量已满时返回 capacity_exceeded
