module: EVENTS_EVENT
contract: Event.archive action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/events/events.ubo.yaml
source_summary: GIVEN 实体 Event 的动作 archive 输入非法 / WHEN 执行动作 archive / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create -> publish -> start -> complete -> archive（活动完整生命周期）；create -> publish -> cancel -> archive（活动发布后取消并归档）
