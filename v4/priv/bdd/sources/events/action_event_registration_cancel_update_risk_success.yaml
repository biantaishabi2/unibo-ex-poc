module: EVENTS_EVENT_REGISTRATION
contract: EventRegistration.cancel action contract (risk:success)
edge_class: action_contract
source_file: models/events/events.ubo.yaml
source_summary: GIVEN 实体 EventRegistration 的动作 cancel 可执行 / WHEN 执行动作 cancel / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：register -> cancel（报名后取消）
