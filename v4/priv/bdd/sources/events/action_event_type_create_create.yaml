module: EVENTS_EVENT_TYPE
contract: EventType.create action contract
edge_class: action_contract
source_file: models/events/events.ubo.yaml
source_summary: GIVEN 实体 EventType 的动作 create 可执行，accept=[name, code, parent_type_id]，args=[无显式参数] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create -> update -> destroy
