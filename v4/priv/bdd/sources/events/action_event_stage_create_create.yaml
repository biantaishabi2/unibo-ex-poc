module: EVENTS_EVENT_STAGE
contract: EventStage.create action contract
edge_class: action_contract
source_file: models/events/events.ubo.yaml
source_summary: GIVEN 实体 EventStage 的动作 create 可执行，accept=[event_id, name, description, start_time, end_time, speaker_name, speaker_id, topic, location, sequence]，args=[无显式参数] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create -> update -> destroy（议程创建、修改、删除）
