module: GAMIFICATION_BADGE_USER
contract: BadgeUser.create action contract
edge_class: action_contract
source_file: models/gamification/gamification.ubo.yaml
source_summary: GIVEN 实体 BadgeUser 的动作 create 可执行，accept=[comment]，args=[user_id:uuid!, sender_id:uuid, badge_id:uuid!, challenge_id:uuid] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应触发关联事件，应符合工作流步骤约束 / 补充场景：create -> destroy
