module: MAINTENANCE_MAINTENANCE_STAGE
contract: MaintenanceStage.create action contract
edge_class: action_contract
source_file: models/maintenance/maintenance.ubo.yaml
source_summary: GIVEN 实体 MaintenanceStage 的动作 create 可执行，accept=[name, sequence, fold, done]，args=[无显式参数] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create 创建维护阶段 -> update 修改阶段配置
