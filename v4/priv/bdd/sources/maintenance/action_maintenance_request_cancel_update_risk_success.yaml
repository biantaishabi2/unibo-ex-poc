module: MAINTENANCE_MAINTENANCE_REQUEST
contract: MaintenanceRequest.cancel action contract (risk:success)
edge_class: action_contract
source_file: models/maintenance/maintenance.ubo.yaml
source_summary: GIVEN 实体 MaintenanceRequest 的动作 cancel 可执行 / WHEN 执行动作 cancel / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 创建维护请求 -> cancel 取消请求
