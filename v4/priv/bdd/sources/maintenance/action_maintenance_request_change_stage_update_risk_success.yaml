module: MAINTENANCE_MAINTENANCE_REQUEST
contract: MaintenanceRequest.change_stage action contract (risk:success)
edge_class: action_contract
source_file: models/maintenance/maintenance.ubo.yaml
source_summary: GIVEN 实体 MaintenanceRequest 的动作 change_stage 可执行 / WHEN 执行动作 change_stage / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 创建维护请求 -> update 更新请求 -> change_stage 变更阶段（完成）
