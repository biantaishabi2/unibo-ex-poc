module: QUALITY_QUALITY_POINT
contract: QualityPoint.create action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/quality/quality.ubo.yaml
source_summary: GIVEN 实体 QualityPoint 的动作 create 输入非法 / WHEN 执行动作 create / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create 创建控制点 -> trigger_checks 自动生成质量检查
