module: QUALITY_QUALITY_REASON
contract: QualityReason.create action contract (risk:change_effect)
edge_class: action_contract
source_file: models/quality/quality.ubo.yaml
source_summary: GIVEN 实体 QualityReason 的动作 create 触发副作用 / WHEN 执行动作 create / THEN 变更副作用应完整生效且状态一致 / 补充场景：create 创建根因 -> update 修改根因描述 -> destroy 删除根因
