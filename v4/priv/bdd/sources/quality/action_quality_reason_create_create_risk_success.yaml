module: QUALITY_QUALITY_REASON
contract: QualityReason.create action contract (risk:success)
edge_class: action_contract
source_file: models/quality/quality.ubo.yaml
source_summary: GIVEN 实体 QualityReason 的动作 create 可执行 / WHEN 执行动作 create / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 创建根因 -> update 修改根因描述 -> destroy 删除根因
