module: QUALITY_QUALITY_CHECK
contract: QualityCheck.create action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/quality/quality.ubo.yaml
source_summary: GIVEN 实体 QualityCheck 的动作 create 输入非法 / WHEN 执行动作 create / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create 创建检查 -> do_pass 检查通过；create 创建检查 -> do_fail 检查失败（自动创建警报）
