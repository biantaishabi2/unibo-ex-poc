module: QUALITY_QUALITY_POINT
contract: QualityPoint.trigger_checks action contract (risk:success)
edge_class: action_contract
source_file: models/quality/quality.ubo.yaml
source_summary: GIVEN 实体 QualityPoint 的动作 trigger_checks 可执行 / WHEN 执行动作 trigger_checks / THEN 成功路径应满足业务约束并持久化结果
