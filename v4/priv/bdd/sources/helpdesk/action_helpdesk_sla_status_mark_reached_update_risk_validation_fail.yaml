module: HELPDESK_HELPDESK_SLA_STATUS
contract: HelpdeskSLAStatus.mark_reached action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/helpdesk/helpdesk.ubo.yaml
source_summary: GIVEN 实体 HelpdeskSLAStatus 的动作 mark_reached 输入非法 / WHEN 执行动作 mark_reached / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：mark_reached 尝试对已 reached 的记录操作，校验失败（终态不可逆）
