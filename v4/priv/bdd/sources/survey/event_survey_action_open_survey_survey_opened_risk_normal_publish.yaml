module: SURVEY_SURVEY
contract: Survey.action_open event contract (risk:normal_publish)
edge_class: event_contract
source_file: models/survey/survey.ubo.yaml
source_summary: GIVEN 实体 Survey 动作 action_open 触发事件 / WHEN 发布 topic `survey.survey.opened` / THEN 消费方应收到一次有效通知
