module: SURVEY_USER_INPUT
contract: UserInput.mark_done event contract
edge_class: event_contract
source_file: models/survey/survey.ubo.yaml
source_summary: GIVEN 实体 UserInput 的动作 mark_done 完成 / WHEN 触发事件发布 / THEN 应广播 topic `survey.user_input.done`
