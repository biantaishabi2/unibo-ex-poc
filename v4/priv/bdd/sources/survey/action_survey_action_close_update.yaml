module: SURVEY_SURVEY
contract: Survey.action_close action contract
edge_class: action_contract
source_file: models/survey/survey.ubo.yaml
source_summary: GIVEN 实体 Survey 的动作 action_close 可执行，accept=[[]]，args=[无显式参数] / WHEN 执行动作 action_close / THEN 输入应满足校验规则，状态变更与副作用应生效，应触发关联事件，应符合工作流步骤约束 / 补充场景：create 问卷，update 修改配置，action_open 发布，action_close 关闭；action_close 关闭后 action_draft 重置为草稿，再次 action_open 发布
