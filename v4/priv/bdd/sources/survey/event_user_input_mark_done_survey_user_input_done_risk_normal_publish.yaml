module: SURVEY_USER_INPUT
contract: UserInput.mark_done event contract (risk:normal_publish)
edge_class: event_contract
source_file: models/survey/survey.ubo.yaml
source_summary: GIVEN 实体 UserInput 动作 mark_done 触发事件 / WHEN 发布 topic `survey.user_input.done` / THEN 消费方应收到一次有效通知
