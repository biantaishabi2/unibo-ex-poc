module: SURVEY_USER_INPUT_LINE
contract: UserInputLine.create action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/survey/survey.ubo.yaml
source_summary: GIVEN 实体 UserInputLine 的动作 create 输入非法 / WHEN 执行动作 create / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create 答案行记录作答内容，destroy 删除旧答案（replace 语义）
