module: DOCUMENTS_WORKFLOW_RULE
contract: WorkflowRule.execute action contract (risk:change_effect)
edge_class: action_contract
source_file: models/documents/documents.ubo.yaml
source_summary: GIVEN 实体 WorkflowRule 的动作 execute 触发副作用 / WHEN 执行动作 execute / THEN 变更副作用应完整生效且状态一致
