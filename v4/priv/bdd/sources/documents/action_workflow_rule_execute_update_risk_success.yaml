module: DOCUMENTS_WORKFLOW_RULE
contract: WorkflowRule.execute action contract (risk:success)
edge_class: action_contract
source_file: models/documents/documents.ubo.yaml
source_summary: GIVEN 实体 WorkflowRule 的动作 execute 可执行 / WHEN 执行动作 execute / THEN 成功路径应满足业务约束并持久化结果
