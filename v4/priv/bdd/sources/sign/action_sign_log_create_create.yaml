module: SIGN_SIGN_LOG
contract: SignLog.create action contract
edge_class: action_contract
source_file: models/sign/sign.ubo.yaml
source_summary: GIVEN 实体 SignLog 的动作 create 可执行，accept=[action, ip_address, user_agent, note, metadata]，args=[request_id:uuid!, request_item_id:uuid, actor_id:uuid] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create -> read
