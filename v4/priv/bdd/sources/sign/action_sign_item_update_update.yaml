module: SIGN_SIGN_ITEM
contract: SignItem.update action contract
edge_class: action_contract
source_file: models/sign/sign.ubo.yaml
source_summary: GIVEN 实体 SignItem 的动作 update 可执行，accept=[type, page, pos_x, pos_y, width, height, required, placeholder, role_id]，args=[无显式参数] / WHEN 执行动作 update / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束
