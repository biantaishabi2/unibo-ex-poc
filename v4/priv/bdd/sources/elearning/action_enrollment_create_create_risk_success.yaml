module: E_LEARNING_ENROLLMENT
contract: Enrollment.create action contract (risk:success)
edge_class: action_contract
source_file: models/elearning/elearning.ubo.yaml
source_summary: GIVEN 实体 Enrollment 的动作 create 可执行 / WHEN 执行动作 create / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 自由注册加入课程（enroll=public），recompute_completion 随学习进度更新；create 邀请注册（member_status=invited），join 接受邀请加入；create 未完成前置课程时校验失败
