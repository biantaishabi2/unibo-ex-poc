module: E_LEARNING_ENROLLMENT
contract: Enrollment.join action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/elearning/elearning.ubo.yaml
source_summary: GIVEN 实体 Enrollment 的动作 join 输入非法 / WHEN 执行动作 join / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：join 尝试对非 invited 状态执行加入，校验失败
