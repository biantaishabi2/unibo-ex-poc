module: POS_POS_ORDER
contract: PosOrder.refund action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/pos/pos.ubo.yaml
source_summary: GIVEN 实体 PosOrder 的动作 refund 输入非法 / WHEN 执行动作 refund / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：refund 创建退款单（部分退款），生成镜像订单
