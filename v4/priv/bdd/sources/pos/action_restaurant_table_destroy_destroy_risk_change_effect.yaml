module: POS_RESTAURANT_TABLE
contract: RestaurantTable.destroy action contract (risk:change_effect)
edge_class: action_contract
source_file: models/pos/pos.ubo.yaml
source_summary: GIVEN 实体 RestaurantTable 的动作 destroy 触发副作用 / WHEN 执行动作 destroy / THEN 变更副作用应完整生效且状态一致 / 补充场景：destroy 关联配置存在活跃会话时删除，校验失败
