module: POS_POS_SESSION
contract: PosSession.start_close action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/pos/pos.ubo.yaml
source_summary: GIVEN 实体 PosSession 的动作 start_close 输入非法 / WHEN 执行动作 start_close / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：start_close 存在 draft 订单时尝试开始关闭，校验失败
