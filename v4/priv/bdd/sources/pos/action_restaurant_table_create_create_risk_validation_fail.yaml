module: POS_RESTAURANT_TABLE
contract: RestaurantTable.create action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/pos/pos.ubo.yaml
source_summary: GIVEN 实体 RestaurantTable 的动作 create 输入非法 / WHEN 执行动作 create / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create 新建餐桌，update 修改座位数和位置；create 新建餐桌，destroy 删除（无活跃会话）；create 座位数为0时校验失败
