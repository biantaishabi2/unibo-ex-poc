module: POS_POS_SESSION
contract: PosSession.create action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/pos/pos.ubo.yaml
source_summary: GIVEN 实体 PosSession 的动作 create 输入非法 / WHEN 执行动作 create / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create 新建会话，open 打开会话（继承期初余额），正常收银；create 新建救援会话（rescue=true），跳过唯一活跃会话校验
