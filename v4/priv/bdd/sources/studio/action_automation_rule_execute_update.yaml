module: STUDIO_AUTOMATION_RULE
contract: AutomationRule.execute action contract
edge_class: action_contract
source_file: models/studio/studio.ubo.yaml
source_summary: GIVEN 实体 AutomationRule 的动作 execute 可执行，accept=[[]]，args=[无显式参数] / WHEN 执行动作 execute / THEN 状态变更与副作用应生效，应符合工作流步骤约束
