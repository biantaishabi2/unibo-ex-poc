module: STUDIO_CUSTOM_VIEW
contract: CustomView.create action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/studio/studio.ubo.yaml
source_summary: GIVEN 实体 CustomView 的动作 create 输入非法 / WHEN 执行动作 create / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create 创建视图 -> update 修改视图布局 -> destroy 删除视图
