module: STUDIO_AUTOMATION_RULE
contract: AutomationRule.execute action contract (risk:change_effect)
edge_class: action_contract
source_file: models/studio/studio.ubo.yaml
source_summary: GIVEN 实体 AutomationRule 的动作 execute 触发副作用 / WHEN 执行动作 execute / THEN 变更副作用应完整生效且状态一致
