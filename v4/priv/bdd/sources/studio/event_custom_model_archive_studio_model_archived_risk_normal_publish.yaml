module: STUDIO_CUSTOM_MODEL
contract: CustomModel.archive event contract (risk:normal_publish)
edge_class: event_contract
source_file: models/studio/studio.ubo.yaml
source_summary: GIVEN 实体 CustomModel 动作 archive 触发事件 / WHEN 发布 topic `studio.model.archived` / THEN 消费方应收到一次有效通知
