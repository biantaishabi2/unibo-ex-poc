module: STUDIO_APP
contract: App.create action contract
edge_class: action_contract
source_file: models/studio/studio.ubo.yaml
source_summary: GIVEN 实体 App 的动作 create 可执行，accept=[name, technical_name, icon, color, description, menu_config, model_ids, version, access_role_ids]，args=[无显式参数] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应触发关联事件，应符合工作流步骤约束 / 补充场景：create 创建应用 -> update 修改应用配置 -> publish 发布应用；create 创建应用 -> publish 发布 -> unpublish 取消发布 -> destroy 删除应用
