module: STUDIO_CUSTOM_MODEL
contract: CustomModel.activate event contract (risk:normal_publish)
edge_class: event_contract
source_file: models/studio/studio.ubo.yaml
source_summary: GIVEN 实体 CustomModel 动作 activate 触发事件 / WHEN 发布 topic `studio.model.activated` / THEN 消费方应收到一次有效通知
