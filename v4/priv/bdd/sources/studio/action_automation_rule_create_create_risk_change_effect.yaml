module: STUDIO_AUTOMATION_RULE
contract: AutomationRule.create action contract (risk:change_effect)
edge_class: action_contract
source_file: models/studio/studio.ubo.yaml
source_summary: GIVEN 实体 AutomationRule 的动作 create 触发副作用 / WHEN 执行动作 create / THEN 变更副作用应完整生效且状态一致 / 补充场景：create 创建自动化规则 -> update 修改规则配置 -> execute 执行规则；create 创建自动化规则 -> destroy 删除规则
