module: STUDIO_CUSTOM_MODEL
contract: CustomModel.reactivate action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/studio/studio.ubo.yaml
source_summary: GIVEN 实体 CustomModel 的动作 reactivate 输入非法 / WHEN 执行动作 reactivate / THEN 应返回校验失败且不产生持久化副作用
