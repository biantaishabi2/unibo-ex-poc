module: ACCOUNTING_BUDGET_ITEM
contract: BudgetItem.create action contract (risk:success)
edge_class: action_contract
source_file: models/accounting/accounting.ubo.yaml
source_summary: GIVEN 实体 BudgetItem 的动作 create 可执行 / WHEN 执行动作 create / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 预算项填写金额和用途，update 调整金额后预算总额自动重算
