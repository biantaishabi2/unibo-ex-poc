module: ACCOUNTING_PAYMENT
contract: Payment.create action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/accounting/accounting.ubo.yaml
source_summary: 'GIVEN 实体 Payment 的动作 create 输入非法 / WHEN 执行动作 create / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create 后 post 过账，再通过 PaymentApplication 核销到发票；create 时 amount 为负被 compare(greater_than_or_equal_to: 0) 校验拒绝 [R-PAY-001]'
