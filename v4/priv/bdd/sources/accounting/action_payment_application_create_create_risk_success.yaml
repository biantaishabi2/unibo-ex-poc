module: ACCOUNTING_PAYMENT_APPLICATION
contract: PaymentApplication.create action contract (risk:success)
edge_class: action_contract
source_file: models/accounting/accounting.ubo.yaml
source_summary: GIVEN 实体 PaymentApplication 的动作 create 可执行 / WHEN 执行动作 create / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 将付款核销到发票，destroy 撤销核销关系
