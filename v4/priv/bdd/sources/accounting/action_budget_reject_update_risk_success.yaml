module: ACCOUNTING_BUDGET
contract: Budget.reject action contract (risk:success)
edge_class: action_contract
source_file: models/accounting/accounting.ubo.yaml
source_summary: GIVEN 实体 Budget 的动作 reject 可执行 / WHEN 执行动作 reject / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：submit 后 reject 驳回，可修改后再次 submit
