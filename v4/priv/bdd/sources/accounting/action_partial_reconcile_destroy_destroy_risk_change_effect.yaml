module: ACCOUNTING_PARTIAL_RECONCILE
contract: PartialReconcile.destroy action contract (risk:change_effect)
edge_class: action_contract
source_file: models/accounting/accounting.ubo.yaml
source_summary: GIVEN 实体 PartialReconcile 的动作 destroy 触发副作用 / WHEN 执行动作 destroy / THEN 变更副作用应完整生效且状态一致 / 补充场景：destroy 撤销部分核销后重算发票 payment_state
