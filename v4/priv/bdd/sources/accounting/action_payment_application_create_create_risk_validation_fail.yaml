module: ACCOUNTING_PAYMENT_APPLICATION
contract: PaymentApplication.create action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/accounting/accounting.ubo.yaml
source_summary: GIVEN 实体 PaymentApplication 的动作 create 输入非法 / WHEN 执行动作 create / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create 将付款核销到发票，destroy 撤销核销关系
