module: ACCOUNTING_PAYMENT
contract: Payment.create action contract (risk:success)
edge_class: action_contract
source_file: models/accounting/accounting.ubo.yaml
source_summary: 'GIVEN 实体 Payment 的动作 create 可执行 / WHEN 执行动作 create / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 后 post 过账，再通过 PaymentApplication 核销到发票；create 时 amount 为负被 compare(greater_than_or_equal_to: 0) 校验拒绝 [R-PAY-001]'
