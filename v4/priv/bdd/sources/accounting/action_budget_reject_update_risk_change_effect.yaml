module: ACCOUNTING_BUDGET
contract: Budget.reject action contract (risk:change_effect)
edge_class: action_contract
source_file: models/accounting/accounting.ubo.yaml
source_summary: GIVEN 实体 Budget 的动作 reject 触发副作用 / WHEN 执行动作 reject / THEN 变更副作用应完整生效且状态一致 / 补充场景：submit 后 reject 驳回，可修改后再次 submit
