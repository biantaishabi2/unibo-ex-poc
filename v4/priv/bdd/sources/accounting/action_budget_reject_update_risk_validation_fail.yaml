module: ACCOUNTING_BUDGET
contract: Budget.reject action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/accounting/accounting.ubo.yaml
source_summary: GIVEN 实体 Budget 的动作 reject 输入非法 / WHEN 执行动作 reject / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：submit 后 reject 驳回，可修改后再次 submit
