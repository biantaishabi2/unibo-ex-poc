module: BARCODE_BARCODE_MAPPING
contract: BarcodeMapping.register action contract (risk:change_effect)
edge_class: action_contract
source_file: models/barcode/barcode.ubo.yaml
source_summary: GIVEN 实体 BarcodeMapping 的动作 register 触发副作用 / WHEN 执行动作 register / THEN 变更副作用应完整生效且状态一致 / 补充场景：register 重复barcode_value -> 拒绝，返回:barcode_already_registered
