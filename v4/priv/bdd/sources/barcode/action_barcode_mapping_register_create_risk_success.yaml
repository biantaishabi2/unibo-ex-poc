module: BARCODE_BARCODE_MAPPING
contract: BarcodeMapping.register action contract (risk:success)
edge_class: action_contract
source_file: models/barcode/barcode.ubo.yaml
source_summary: GIVEN 实体 BarcodeMapping 的动作 register 可执行 / WHEN 执行动作 register / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：register 重复barcode_value -> 拒绝，返回:barcode_already_registered
