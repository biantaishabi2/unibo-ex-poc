module: BARCODE_BARCODE_RULE
contract: BarcodeRule.reorder action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/barcode/barcode.ubo.yaml
source_summary: GIVEN 实体 BarcodeRule 的动作 reorder 输入非法 / WHEN 执行动作 reorder / THEN 应返回校验失败且不产生持久化副作用
