module: BARCODE_BARCODE_RULE
contract: BarcodeRule.update action contract
edge_class: action_contract
source_file: models/barcode/barcode.ubo.yaml
source_summary: GIVEN 实体 BarcodeRule 的动作 update 可执行，accept=[name, sequence, encoding, barcode_type, pattern, alias, gs1_content_type, gs1_decimal_usage]，args=[无显式参数] / WHEN 执行动作 update / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束
