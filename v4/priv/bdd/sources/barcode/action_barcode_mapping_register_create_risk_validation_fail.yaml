module: BARCODE_BARCODE_MAPPING
contract: BarcodeMapping.register action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/barcode/barcode.ubo.yaml
source_summary: GIVEN 实体 BarcodeMapping 的动作 register 输入非法 / WHEN 执行动作 register / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：register 重复barcode_value -> 拒绝，返回:barcode_already_registered
