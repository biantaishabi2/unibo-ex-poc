module: BARCODE_BARCODE_RULE
contract: BarcodeRule.create action contract (risk:change_effect)
edge_class: action_contract
source_file: models/barcode/barcode.ubo.yaml
source_summary: 'GIVEN 实体 BarcodeRule 的动作 create 触发副作用 / WHEN 执行动作 create / THEN 变更副作用应完整生效且状态一致 / 补充场景：create 创建EAN-13产品规则 -> update 修改匹配模式 -> reorder 调整优先级；create{encoding: gs1_128, gs1_content_type: identifier} 创建GS1规则 -> 关联AI定义'
