module: PLM_BOM_REVISION
contract: BomRevision.bom_component_delta_impact workflow contract (risk:mainline)
edge_class: workflow_contract
source_file: models/plm/plm.ubo.yaml
source_summary: GIVEN 实体 BomRevision 定义工作流 bom_component_delta_impact / WHEN 按主路径执行 detect_component_delta -> sync_procurement_stock_impact / THEN 主流程应可达并保持顺序一致 / 补充场景：detect_component_delta 检测组件增删 -> sync_procurement_stock_impact 同步供应链影响；新增组件→采购需求，删除组件→库存消化
