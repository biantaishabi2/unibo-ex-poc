module: PLM_ECO
contract: Eco.create event contract (risk:normal_publish)
edge_class: event_contract
source_file: models/plm/plm.ubo.yaml
source_summary: GIVEN 实体 Eco 动作 create 触发事件 / WHEN 发布 topic `plm.eco.created` / THEN 消费方应收到一次有效通知
