module: PLM_ECO
contract: Eco.apply_changes event contract (risk:normal_publish)
edge_class: event_contract
source_file: models/plm/plm.ubo.yaml
source_summary: GIVEN 实体 Eco 动作 apply_changes 触发事件 / WHEN 发布 topic `plm.eco.changes_applied` / THEN 消费方应收到一次有效通知
