module: PLM_ECO_APPROVAL
contract: EcoApproval.create action contract (risk:success)
edge_class: action_contract
source_file: models/plm/plm.ubo.yaml
source_summary: GIVEN 实体 EcoApproval 的动作 create 可执行 / WHEN 执行动作 create / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 创建审批记录 -> reject 审批拒绝
