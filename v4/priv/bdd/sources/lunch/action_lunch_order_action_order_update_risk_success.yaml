module: LUNCH_LUNCH_ORDER
contract: LunchOrder.action_order action contract (risk:success)
edge_class: action_contract
source_file: models/lunch/lunch.ubo.yaml
source_summary: GIVEN 实体 LunchOrder 的动作 action_order 可执行 / WHEN 执行动作 action_order / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：action_order 下单时供应商不可用，校验失败；action_order 下单时钱包余额不足，校验失败
