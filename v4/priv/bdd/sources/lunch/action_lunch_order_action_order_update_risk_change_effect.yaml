module: LUNCH_LUNCH_ORDER
contract: LunchOrder.action_order action contract (risk:change_effect)
edge_class: action_contract
source_file: models/lunch/lunch.ubo.yaml
source_summary: GIVEN 实体 LunchOrder 的动作 action_order 触发副作用 / WHEN 执行动作 action_order / THEN 变更副作用应完整生效且状态一致 / 补充场景：action_order 下单时供应商不可用，校验失败；action_order 下单时钱包余额不足，校验失败
