module: LUNCH_LUNCH_SUPPLIER
contract: LunchSupplier.send_orders event contract (risk:normal_publish)
edge_class: event_contract
source_file: models/lunch/lunch.ubo.yaml
source_summary: GIVEN 实体 LunchSupplier 动作 send_orders 触发事件 / WHEN 发布 topic `lunch.supplier.orders_sent` / THEN 消费方应收到一次有效通知
