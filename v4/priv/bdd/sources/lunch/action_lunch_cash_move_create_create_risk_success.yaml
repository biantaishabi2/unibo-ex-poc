module: LUNCH_LUNCH_CASH_MOVE
contract: LunchCashMove.create action contract (risk:success)
edge_class: action_contract
source_file: models/lunch/lunch.ubo.yaml
source_summary: GIVEN 实体 LunchCashMove 的动作 create 可执行 / WHEN 执行动作 create / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 新建充值流水（正数），destroy 删除流水；create 新建消费流水（负数），destroy 删除流水
