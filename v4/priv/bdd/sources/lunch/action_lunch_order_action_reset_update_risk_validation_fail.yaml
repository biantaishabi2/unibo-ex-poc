module: LUNCH_LUNCH_ORDER
contract: LunchOrder.action_reset action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/lunch/lunch.ubo.yaml
source_summary: GIVEN 实体 LunchOrder 的动作 action_reset 输入非法 / WHEN 执行动作 action_reset / THEN 应返回校验失败且不产生持久化副作用
