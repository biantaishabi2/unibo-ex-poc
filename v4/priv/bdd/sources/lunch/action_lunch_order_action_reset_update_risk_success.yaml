module: LUNCH_LUNCH_ORDER
contract: LunchOrder.action_reset action contract (risk:success)
edge_class: action_contract
source_file: models/lunch/lunch.ubo.yaml
source_summary: GIVEN 实体 LunchOrder 的动作 action_reset 可执行 / WHEN 执行动作 action_reset / THEN 成功路径应满足业务约束并持久化结果
