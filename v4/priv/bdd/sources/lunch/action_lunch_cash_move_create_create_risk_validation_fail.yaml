module: LUNCH_LUNCH_CASH_MOVE
contract: LunchCashMove.create action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/lunch/lunch.ubo.yaml
source_summary: GIVEN 实体 LunchCashMove 的动作 create 输入非法 / WHEN 执行动作 create / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create 新建充值流水（正数），destroy 删除流水；create 新建消费流水（负数），destroy 删除流水
