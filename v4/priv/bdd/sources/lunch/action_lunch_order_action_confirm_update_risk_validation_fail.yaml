module: LUNCH_LUNCH_ORDER
contract: LunchOrder.action_confirm action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/lunch/lunch.ubo.yaml
source_summary: GIVEN 实体 LunchOrder 的动作 action_confirm 输入非法 / WHEN 执行动作 action_confirm / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：create 新建订单，action_order 下单，action_send 发送，action_confirm 确认；action_confirm 尝试对非 sent 状态确认，校验失败
