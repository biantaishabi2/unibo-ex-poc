module: LUNCH_LUNCH_LOCATION
contract: LunchLocation.create action contract (risk:change_effect)
edge_class: action_contract
source_file: models/lunch/lunch.ubo.yaml
source_summary: GIVEN 实体 LunchLocation 的动作 create 触发副作用 / WHEN 执行动作 create / THEN 变更副作用应完整生效且状态一致 / 补充场景：create 新建配送地点，update 修改名称和地址
