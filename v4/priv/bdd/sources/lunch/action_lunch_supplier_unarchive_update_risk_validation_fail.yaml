module: LUNCH_LUNCH_SUPPLIER
contract: LunchSupplier.unarchive action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/lunch/lunch.ubo.yaml
source_summary: GIVEN 实体 LunchSupplier 的动作 unarchive 输入非法 / WHEN 执行动作 unarchive / THEN 应返回校验失败且不产生持久化副作用 / 补充场景：unarchive 尝试对启用状态供应商取消归档，校验失败
