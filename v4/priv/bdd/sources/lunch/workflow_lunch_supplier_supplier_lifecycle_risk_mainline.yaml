module: LUNCH_LUNCH_SUPPLIER
contract: LunchSupplier.supplier_lifecycle workflow contract (risk:mainline)
edge_class: workflow_contract
source_file: models/lunch/lunch.ubo.yaml
source_summary: GIVEN 实体 LunchSupplier 定义工作流 supplier_lifecycle / WHEN 按主路径执行 create -> update -> send_orders -> confirm_orders -> archive -> unarchive / THEN 主流程应可达并保持顺序一致
