defmodule UniboV4Web.Generated.Schema.Engagement do
  @moduledoc "自动生成的 engagement 子域 GraphQL Schema — 由 UniBO 编译器生成，请勿手动编辑"
  use Absinthe.Schema

  use AshGraphql,
    domains: [
      UniboV4.Gamification,
      UniboV4.Membership,
    ]

  query do
  end

  mutation do
  end
end
