# 模型业务说明

- 版本：1.0
- 领域：工作任务
- 领域说明：从 OFBiz 导入的 WorkEffort 领域模型
- 实体数量：69

## 实体：时间凭证（聚合根）

- 表名：time_entries

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 时间凭证编号：文本
- 来源日期：日期时间
- 至日期：日期时间
- 发票项序列编号：文本
- 工时：浮点
- 评论：文本

#### 关系
- 参与方：多对一 -> 参与方，外键 参与方编号
- 费率类型：多对一 -> 费率类型，外键 费率类型编号
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 工时表：多对一 -> 工时表，外键 工时表编号
- 发票：多对一 -> 发票，外键 发票编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：工时表（聚合根）

- 表名：timesheets

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 工时表编号：文本
- 来源日期：日期时间
- 至日期：日期时间
- 评论：文本

#### 关系
- 参与方：多对一 -> 参与方，外键 参与方编号
- 客户参与方：多对一 -> 参与方，外键 客户参与方编号
- 状态项：多对一 -> 状态项，外键 状态编号
- 已审批用户登录：多对一 -> 用户登录，外键 已审批用户登录编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：工时表角色（聚合根）

- 表名：timesheet_roles

### Schema（数据模型）

#### 关系
- 工时表：多对一 -> 工时表，外键 工时表编号
- 参与方：多对一 -> 参与方，外键 参与方编号
- 角色类型：多对一 -> 角色类型，外键 角色类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工时表编号 设为 工时表编号

## 实体：应用沙箱（聚合根）

- 表名：application_sandboxes

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 申请编号：文本
- 工作工作量编号：文本
- 参与方编号：文本
- 角色类型编号：文本
- 来源日期：日期时间

#### 关系
- 运行时数据：多对一 -> 运行时数据，外键 运行时数据编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：通信事件工作任务（聚合根）

- 表名：communication_event_work_effs

### Schema（数据模型）

#### 字段
- 说明：文本

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 沟通活动：多对一 -> 沟通活动，外键 沟通活动编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：交付物（聚合根）

- 表名：deliverables

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 交付物编号：文本
- 交付物名称：文本
- 说明：文本

#### 关系
- 交付物类型：多对一 -> 交付物类型，外键 交付物类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：交付物类型（聚合根）

- 表名：deliverable_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 交付物类型编号：文本
- 说明：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：工作任务（聚合根）

- 表名：work_efforts

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 工作工作量编号：文本
- 最近状态更新：日期时间
- 优先级：整数
- 百分比完成：整数
- 工作工作量名称：文本
- 显示枚举编号：文本
- 发送通知邮箱：布尔
- 说明：文本
- 位置说明：文本
- 预估开始日期：日期时间
- 预估完成度日期：日期时间
- 实际开始日期：日期时间
- 实际完成度日期：日期时间
- 预估毫秒：浮点
- 预估准备毫秒：浮点
- 实际毫秒：浮点
- 实际准备毫秒：浮点
- 合计毫秒允许：浮点
- 合计资金允许：金额
- 特别条款：文本
- 已弃用 - 改用分配实体中的 availabilityStatusId 字段（time_transparency）：整数
- 通用编号：文本
- 来源参考号编号：文本
- 信息链接：文本
- 服务加载器名称：文本
- 数量生产：金额
- 数量已生产：金额
- 数量已驳回：金额
- 预留人数：金额，说明 租赁关联资产的人数
- 第二人预留人均百分比：金额，说明 预订第二人价格百分比：与工作任务关联的此资产第二个租赁人的最终价格百分比
- 预留第N人人均百分比：金额，说明 预订第N人价格百分比：与工作任务关联的此资产第N个（2+）租赁人的最终价格百分比
- 版本单号：整数
- 创建日期：日期时间
- 创建用户登录：文本
- 最近修改日期：日期时间
- 最近修改用户登录：文本

#### 关系
- 工作工作量类型：多对一 -> 工作任务类型，外键 工作工作量类型编号
- 工作工作量用途类型：多对一 -> 工作任务用途类型，外键 工作工作量用途类型编号
- 上级工作工作量：多对一 -> 工作任务，外键 工作工作量上级编号
- 当前状态项：多对一 -> 状态项，外键 当前状态编号
- 范围枚举：多对一 -> 枚举，外键 范围枚举编号
- 固定资产：多对一 -> 固定资产，外键 固定资产编号
- 设施：多对一 -> 设施，外键 设施编号
- 资金单位：多对一 -> 单位，外键 资金单位编号
- 重复周期信息：多对一 -> 重复周期信息，外键 重复周期信息编号
- 时间表达式：多对一 -> 时间表达式，外键 临时表达式编号
- 运行时数据：多对一 -> 运行时数据，外键 运行时数据编号
- 备注数据：多对一 -> 备注数据，外键 备注编号
- 自定义规则方法：多对一 -> 自定义规则方法，外键 预估计算方法
- 住宿映射：多对一 -> 住宿地图，外键 住宿映射编号
- 住宿位置：多对一 -> 住宿位置，外键 住宿位置编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：工作任务关联（聚合根）

- 表名：work_effort_assocs

### Schema（数据模型）

#### 字段
- 序列编号：整数
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间

#### 关系
- 工作工作量关联类型：多对一 -> 工作任务关联类型，外键 工作工作量关联类型编号
- 来源工作工作量：多对一 -> 工作任务，外键 工作工作量编号来源
- 工作工作量：多对一 -> 工作任务，外键 工作工作量编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号来源 设为 工作工作量编号来源

## 实体：工作任务关联属性（聚合根）

- 表名：work_effort_assoc_attributes

### Schema（数据模型）

#### 字段
- 工作工作量编号来源：文本（必填、主键）
- 工作工作量编号：文本（必填、主键）
- 工作工作量关联类型编号：文本（必填、主键）
- 来源日期：日期时间
- 属性名称：文本（必填、主键）
- 属性值：文本
- 属性说明：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号来源 设为 工作工作量编号来源

## 实体：工作任务关联类型（聚合根）

- 表名：work_effort_assoc_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 工作工作量关联类型编号：文本
- 表：布尔
- 说明：文本

#### 关系
- 上级工作工作量关联类型：多对一 -> 工作任务关联类型，外键 上级类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：工作任务关联类型属性（聚合根）

- 表名：work_effort_assoc_type_attrs

### Schema（数据模型）

#### 字段
- 属性名称：文本（必填、主键）
- 说明：文本

#### 关系
- 工作工作量关联类型：多对一 -> 工作任务关联类型，外键 工作工作量关联类型编号
- 工作工作量关联属性：一对多 -> 工作任务关联属性，外键 工作工作量关联类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量关联类型编号 设为 工作工作量关联类型编号

## 实体：工作任务属性（聚合根）

- 表名：work_effort_attributes

### Schema（数据模型）

#### 字段
- 属性名称：文本（必填、主键）
- 属性值：文本
- 属性说明：文本

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务计费（聚合根）

- 表名：work_effort_billings

### Schema（数据模型）

#### 字段
- 发票项序列编号：文本（必填、主键）
- 百分比：浮点

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 发票：多对一 -> 发票，外键 发票编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务联系方式（聚合根）

- 表名：work_effort_contact_meches

### Schema（数据模型）

#### 字段
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间
- 评论：文本

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 联系方式：多对一 -> 联系方式，外键 联系方式编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务内容（聚合根）

- 表名：work_effort_contents

### Schema（数据模型）

#### 字段
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 内容：多对一 -> 内容，外键 内容编号
- 工作工作量内容类型：多对一 -> 工作任务内容类型，外键 工作工作量内容类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务内容类型（聚合根）

- 表名：work_effort_content_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 工作工作量内容类型编号：文本
- 说明：文本

#### 关系
- 上级工作工作量内容类型：多对一 -> 工作任务内容类型，外键 上级类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：工作任务交付物产出（聚合根）

- 表名：work_effort_deliverable_prods

### Schema（数据模型）

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 交付物：多对一 -> 交付物，外键 交付物编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务事件提醒（聚合根）

- 表名：work_effort_event_reminders

### Schema（数据模型）

#### 字段
- 序列编号：文本（必填、主键）
- 提醒日期时间：日期时间
- 重复数量：整数
- 重复间隔：整数，说明 提醒重复之间的毫秒间隔
- 当前数量：整数
- 提醒偏移：整数，说明 从事件到激活提醒的毫秒偏移
- 区域编号：文本
- 时间区域编号：文本

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 联系方式：多对一 -> 联系方式，外键 联系方式编号
- 参与方：多对一 -> 参与方，外键 参与方编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务固定资产分配（聚合根）

- 表名：work_effort_fixed_asset_assigns

### Schema（数据模型）

#### 字段
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间
- 已分配成本：金额
- 评论：文本

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 固定资产：多对一 -> 固定资产，外键 固定资产编号
- 状态项：多对一 -> 状态项，外键 状态编号
- 可用性状态项：多对一 -> 状态项，外键 可用性状态编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务固定资产标准（聚合根）

- 表名：work_effort_fixed_asset_stds

### Schema（数据模型）

#### 字段
- 预估数量：浮点
- 预估时长：浮点
- 预估成本：金额

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 固定资产类型：多对一 -> 固定资产类型，外键 固定资产类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务商品标准（聚合根）

- 表名：work_effort_good_standards

### Schema（数据模型）

#### 字段
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间
- 预估数量：浮点
- 预估成本：金额

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 工作工作量良好标准类型：多对一 -> 工作任务商品标准类型，外键 工作工作量良好标准类型编号
- 产品：多对一 -> 产品，外键 产品编号
- 状态项：多对一 -> 状态项，外键 状态编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务商品标准类型（聚合根）

- 表名：work_effort_good_standard_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 工作工作量良好标准类型编号：文本
- 表：布尔
- 说明：文本

#### 关系
- 上级工作工作量良好标准类型：多对一 -> 工作任务商品标准类型，外键 上级类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：工作任务日历数据（聚合根）

- 表名：work_effort_ical_datas

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- iCalendar 数据（ical_data）：文本

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：工作任务库存分配（聚合根）

- 表名：work_effort_inventory_assigns

### Schema（数据模型）

#### 字段
- 数量：浮点

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 库存项：多对一 -> 库存项，外键 库存项编号
- 状态项：多对一 -> 状态项，外键 状态编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务库存产出（聚合根）

- 表名：work_effort_inventory_produceds

### Schema（数据模型）

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 库存项：多对一 -> 库存项，外键 库存项编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务成本计算（聚合根）

- 表名：work_effort_cost_calcs

### Schema（数据模型）

#### 字段
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 成本组件类型：多对一 -> 成本组件类型，外键 成本组件类型编号
- 成本组件计算：多对一 -> 成本组件计算，外键 成本组件计算编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务关键词（聚合根）

- 表名：work_effort_keywords

### Schema（数据模型）

#### 字段
- 关键词：文本（必填、主键）
- 相关度重量：整数

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务备注（聚合根）

- 表名：work_effort_notes

### Schema（数据模型）

#### 字段
- 内部备注：布尔

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 备注数据：多对一 -> 备注数据，外键 备注编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务参与方分配（聚合根）

- 表名：work_effort_party_assignments

### Schema（数据模型）

#### 字段
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间
- 状态日期时间：日期时间
- 评论：文本
- 必须回复：布尔

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 参与方：多对一 -> 参与方，外键 参与方编号
- 角色类型：多对一 -> 角色类型，外键 角色类型编号
- 已分配用户登录：多对一 -> 用户登录，外键 已分配用户登录编号
- 分配状态项：多对一 -> 状态项，外键 状态编号
- 期望枚举：多对一 -> 枚举，外键 期望枚举编号
- 委派原因枚举：多对一 -> 枚举，外键 委派原因枚举编号
- 设施：多对一 -> 设施，外键 设施编号
- 可用性状态项：多对一 -> 状态项，外键 可用性状态编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务用途类型（聚合根）

- 表名：work_effort_purpose_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 工作工作量用途类型编号：文本
- 说明：文本

#### 关系
- 上级工作工作量用途类型：多对一 -> 工作任务用途类型，外键 上级类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：工作任务评审（聚合根）

- 表名：work_effort_reviews

### Schema（数据模型）

#### 字段
- 审核日期：日期时间（必填、主键）
- 已过账匿名：布尔
- 评级：浮点
- 审核长文本：文本

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 用户登录：多对一 -> 用户登录，外键 用户登录编号
- 状态项：多对一 -> 状态项，外键 状态编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务搜索约束（聚合根）

- 表名：work_effort_search_constraints

### Schema（数据模型）

#### 字段
- 约束序列编号：文本（必填、主键）
- 约束名称：文本
- 信息文本：文本
- 包含子工作任务：布尔
- 且：布尔
- 任意前缀：布尔
- 任意后缀：布尔
- 移除词干：布尔
- 低值：文本
- 高值：文本

#### 关系
- 工作工作量搜索结果：多对一 -> 工作任务搜索结果，外键 工作工作量搜索结果编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量搜索结果编号 设为 工作工作量搜索结果编号

## 实体：工作任务搜索结果（聚合根）

- 表名：work_effort_search_results

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 工作工作量搜索结果编号：文本
- 访问编号：文本
- 订单名称：文本
- 升序：布尔
- 编号结果：整数
- 秒合计：浮点
- 搜索日期：日期时间

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：工作任务技能标准（聚合根）

- 表名：work_effort_skill_standards

### Schema（数据模型）

#### 字段
- 预估编号人数：浮点
- 预估时长：浮点
- 预估成本：金额

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 技能类型：多对一 -> 技能类型，外键 技能类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务状态（聚合根）

- 表名：work_effort_statuses

### Schema（数据模型）

#### 字段
- 状态日期时间：日期时间（必填、主键）
- 原因：文本

#### 关系
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 状态项：多对一 -> 状态项，外键 状态编号
- 设置用户：多对一 -> 用户登录，外键 设置用户登录

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：工作任务转运箱（聚合根）

- 表名：work_effort_trans_boxes

### Schema（数据模型）

#### 字段
- 活动编号：文本（必填、主键）
- 转换编号：文本（必填、主键）

#### 关系
- 工作任务：多对一 -> 工作任务，外键 流程工作工作量编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 流程工作工作量编号 设为 流程工作工作量编号

## 实体：工作任务类型（聚合根）

- 表名：work_effort_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 工作工作量类型编号：文本
- 表：布尔
- 说明：文本

#### 关系
- 上级工作工作量类型：多对一 -> 工作任务类型，外键 上级类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：工作任务类型属性（聚合根）

- 表名：work_effort_type_attrs

### Schema（数据模型）

#### 字段
- 属性名称：文本（必填、主键）
- 说明：文本

#### 关系
- 工作工作量类型：多对一 -> 工作任务类型，外键 工作工作量类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量类型编号 设为 工作工作量类型编号

## 实体：工作任务调研应用（聚合根）

- 表名：work_effort_survey_appls

### Schema（数据模型）

#### 字段
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间

#### 关系
- 调研：多对一 -> 调研，外键 调研编号
- 工作任务：多对一 -> 工作任务，外键 工作工作量编号
- 产品店铺调研应用：多对一 -> 产品店铺调研应用，外键 产品店铺调研应用编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 工作工作量编号 设为 工作工作量编号

## 实体：住宿地图

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：住宿位置

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：沟通活动

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：联系方式

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：内容

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：成本组件计算

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：成本组件类型

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：自定义规则方法

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：枚举

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：设施

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：固定资产

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：固定资产类型

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：库存项

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：发票

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：备注数据

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：参与方

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：产品

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：产品店铺调研应用

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：费率类型

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：重复周期信息

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：角色类型

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：运行时数据

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：技能类型

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：状态项

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：调研

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：电话号码

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：时间表达式

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：单位

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

## 实体：用户登录

- 说明：占位实体（Batch B2 关系补全）

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）

