# 模型业务说明

- 版本：1.0
- 领域：内容
- 领域说明：从 OFBiz 导入的 Content 领域模型
- 实体数量：64

## 实体：内容（聚合根）

- 表名：contents

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 内容编号：文本
- 数据来源编号：文本
- 状态编号：文本
- 权限枚举编号：文本
- 服务名称：文本，说明 已弃用：改用 customMethod 模式。保持向后兼容
- 自定义规则方法编号：文本
- 内容名称：文本
- 说明：文本
- 区域文本：文本
- 下级叶数量：整数
- 下级分支数量：整数
- 创建日期：日期时间
- 创建用户登录：文本
- 最近修改日期：日期时间
- 最近修改用户登录：文本

#### 关系
- 内容类型：多对一 -> 内容类型，外键 内容类型编号
- 数据资源：多对一 -> 数据资源，外键 数据资源编号
- 模板数据资源：多对一 -> 数据资源，外键 模板数据资源编号
- MIME类型：多对一 -> 文件类型，外键 MIME类型编号
- 字符集：多对一 -> 字符集，外键 字符设置编号
- 装饰内容：多对一 -> 内容，外键 装饰内容编号
- 所有者内容：多对一 -> 内容，外键 所有者内容编号
- 实例内容：多对一 -> 内容，外键 实例内容编号
- 内容关联数据资源视图来源：一对多 -> 内容关联，外键 内容编号
- content_assocs_to：一对多 -> 内容关联，外键 内容编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：内容审批（聚合根）

- 表名：content_approvals

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 内容审批编号：文本
- 内容版本序列编号：文本
- 参与方编号：文本
- 角色类型编号：文本
- 审批状态编号：文本
- 审批日期：日期时间
- 序列编号：整数
- 评论：文本

#### 关系
- 内容：多对一 -> 内容，外键 内容编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：内容关联（聚合根）

- 表名：content_assocs

### Schema（数据模型）

#### 字段
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间
- 数据来源编号：文本
- 序列编号：整数
- 映射键：文本
- 上限坐标：整数
- 左坐标：整数
- 创建日期：日期时间
- 创建用户登录：文本
- 最近修改日期：日期时间
- 最近修改用户登录：文本

#### 关系
- 来源内容：多对一 -> 内容，外键 内容编号
- 内容：多对一 -> 内容，外键 内容编号
- 内容关联类型：多对一 -> 内容关联类型，外键 内容关联类型编号
- 内容关联谓词：多对一 -> 内容关联谓词，外键 内容关联谓词编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 内容编号 设为 内容编号

## 实体：内容关联谓词（聚合根）

- 表名：content_assoc_predicates

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 内容关联谓词编号：文本
- 说明：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：内容关联类型（聚合根）

- 表名：content_assoc_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 内容关联类型编号：文本
- 说明：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：内容属性（聚合根）

- 表名：content_attributes

### Schema（数据模型）

#### 字段
- 属性名称：文本（必填、主键）
- 属性值：文本
- 属性说明：文本

#### 关系
- 内容：多对一 -> 内容，外键 内容编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 内容编号 设为 内容编号

## 实体：内容元数据（聚合根）

- 表名：content_meta_datas

### Schema（数据模型）

#### 字段
- 元数据数据值：文本
- 数据来源编号：文本

#### 关系
- 内容：多对一 -> 内容，外键 内容编号
- 元数据谓词：多对一 -> 元数据谓词，外键 元数据数据谓词编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 内容编号 设为 内容编号

## 实体：内容操作（聚合根）

- 表名：content_operations

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 内容操作编号：文本
- 说明：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：内容用途（聚合根）

- 表名：content_purposes

### Schema（数据模型）

#### 字段
- 序列编号：整数

#### 关系
- 内容：多对一 -> 内容，外键 内容编号
- 内容用途类型：多对一 -> 内容用途类型，外键 内容用途类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 内容编号 设为 内容编号

## 实体：内容用途操作（聚合根）

- 表名：content_purpose_operations

### Schema（数据模型）

#### 字段
- 角色类型编号：文本（必填、主键）
- 状态编号：文本（必填、主键）
- 权限枚举编号：文本（必填、主键）

#### 关系
- 内容用途类型：多对一 -> 内容用途类型，外键 内容用途类型编号
- 内容操作：多对一 -> 内容操作，外键 内容操作编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 内容用途类型编号 设为 内容用途类型编号

## 实体：内容用途类型（聚合根）

- 表名：content_purpose_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 内容用途类型编号：文本
- 说明：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：内容版本（聚合根）

- 表名：content_revisions

### Schema（数据模型）

#### 字段
- 内容版本序列编号：文本（必填、主键）
- 已提交参与方编号：文本
- 评论：文本

#### 关系
- 内容：多对一 -> 内容，外键 内容编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 内容编号 设为 内容编号

## 实体：内容版本项（聚合根）

- 表名：content_revision_items

### Schema（数据模型）

#### 字段
- 内容编号：文本（必填、主键）
- 内容版本序列编号：文本（必填、主键）
- 项内容编号：文本（必填、主键）

#### 关系
- 旧数据资源：多对一 -> 数据资源，外键 旧数据资源编号
- 新数据资源：多对一 -> 数据资源，外键 新数据资源编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 内容编号 设为 内容编号

## 实体：内容角色（聚合根）

- 表名：content_roles

### Schema（数据模型）

#### 字段
- 参与方编号：文本（必填、主键）
- 角色类型编号：文本（必填、主键）
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间

#### 关系
- 内容：多对一 -> 内容，外键 内容编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 内容编号 设为 内容编号

## 实体：内容类型（聚合根）

- 表名：content_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 内容类型编号：文本
- 表：布尔
- 说明：文本

#### 关系
- 上级内容类型：多对一 -> 内容类型，外键 上级类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：内容类型属性（聚合根）

- 表名：content_type_attrs

### Schema（数据模型）

#### 字段
- 属性名称：文本（必填、主键）
- 说明：文本

#### 关系
- 内容类型：多对一 -> 内容类型，外键 内容类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 内容类型编号 设为 内容类型编号

## 实体：音频数据资源（聚合根）

- 表名：audio_data_resources

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 音频数据：文本

#### 关系
- 数据资源：多对一 -> 数据资源，外键 数据资源编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：字符集（聚合根）

- 表名：character_sets

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 字符设置编号：文本
- 说明：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：数据分类（聚合根）

- 表名：data_categories

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 数据分类编号：文本
- 分类名称：文本

#### 关系
- 上级数据分类：多对一 -> 数据分类，外键 上级分类编号
- 同级数据分类：一对多 -> 数据分类

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：数据资源（聚合根）

- 表名：data_resources

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 数据资源编号：文本
- 数据来源编号：文本
- 状态编号：文本
- 数据资源名称：文本
- 区域文本：文本
- 短文本的文本放在这里（object_info）：文本
- 关联明细编号：文本，说明 根据 dataResourceTypeId，可以指向其他实体，如：调研、调研响应等
- 公开：布尔，说明 设置为 Y 则任何人都可以下载，否则下载被限制
- 创建日期：日期时间
- 创建用户登录：文本
- 最近修改日期：日期时间
- 最近修改用户登录：文本

#### 关系
- 数据资源类型：多对一 -> 数据资源类型，外键 数据资源类型编号
- 数据模板类型：多对一 -> 数据模板类型，外键 数据模板类型编号
- 数据分类：多对一 -> 数据分类，外键 数据分类编号
- MIME类型：多对一 -> 文件类型，外键 MIME类型编号
- 字符集：多对一 -> 字符集，外键 字符设置编号
- 调研：多对一 -> 调研，外键 调研编号
- 调研响应：多对一 -> 调研响应，外键 调研响应编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：数据资源属性（聚合根）

- 表名：data_resource_attributes

### Schema（数据模型）

#### 字段
- 属性名称：文本（必填、主键）
- 属性值：文本
- 属性说明：文本

#### 关系
- 数据资源：多对一 -> 数据资源，外键 数据资源编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 数据资源编号 设为 数据资源编号

## 实体：数据资源元数据（聚合根）

- 表名：data_resource_meta_datas

### Schema（数据模型）

#### 字段
- 元数据数据值：文本
- 数据来源编号：文本

#### 关系
- 数据资源：多对一 -> 数据资源，外键 数据资源编号
- 元数据谓词：多对一 -> 元数据谓词，外键 元数据数据谓词编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 数据资源编号 设为 数据资源编号

## 实体：数据资源用途（聚合根）

- 表名：data_resource_purposes

### Schema（数据模型）

#### 关系
- 数据资源：多对一 -> 数据资源，外键 数据资源编号
- 内容用途类型：多对一 -> 内容用途类型，外键 内容用途类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 数据资源编号 设为 数据资源编号

## 实体：数据资源角色（聚合根）

- 表名：data_resource_roles

### Schema（数据模型）

#### 字段
- 参与方编号：文本（必填、主键）
- 角色类型编号：文本（必填、主键）
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间

#### 关系
- 数据资源：多对一 -> 数据资源，外键 数据资源编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 数据资源编号 设为 数据资源编号

## 实体：数据资源类型（聚合根）

- 表名：data_resource_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 数据资源类型编号：文本
- 表：布尔
- 说明：文本

#### 关系
- 上级数据资源类型：多对一 -> 数据资源类型，外键 上级类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：数据资源类型属性（聚合根）

- 表名：data_resource_type_attrs

### Schema（数据模型）

#### 字段
- 属性名称：文本（必填、主键）
- 说明：文本

#### 关系
- 数据资源类型：多对一 -> 数据资源类型，外键 数据资源类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 数据资源类型编号 设为 数据资源类型编号

## 实体：数据模板类型（聚合根）

- 表名：data_template_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 数据模板类型编号：文本
- 说明：文本
- 扩展：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：电子文本（聚合根）

- 表名：electronic_texts

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 长文本数据：文本

#### 关系
- 数据资源：多对一 -> 数据资源，外键 数据资源编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：文件扩展（聚合根）

- 表名：file_extensions

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 文件扩展编号：文本

#### 关系
- MIME类型：多对一 -> 文件类型，外键 MIME类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：图片数据资源（聚合根）

- 表名：image_data_resources

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 图片数据：文本

#### 关系
- 数据资源：多对一 -> 数据资源，外键 数据资源编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：元数据谓词（聚合根）

- 表名：meta_data_predicates

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 元数据数据谓词编号：文本
- 说明：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：文件类型（聚合根）

- 表名：mime_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- MIME类型编号：文本
- 说明：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：MIME类型HTML模板（聚合根）

- 表名：mime_type_html_templates

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 模板位置：文本

#### 关系
- MIME类型：多对一 -> 文件类型，外键 MIME类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：其他数据资源（聚合根）

- 表名：other_data_resources

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 数据资源内容：文本

#### 关系
- 数据资源：多对一 -> 数据资源，外键 数据资源编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：视频数据资源（聚合根）

- 表名：video_data_resources

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 视频数据：文本

#### 关系
- 数据资源：多对一 -> 数据资源，外键 数据资源编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：文档（聚合根）

- 表名：documents

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 文档编号：文本
- 日期创建：日期时间
- 评论：文本
- 文档位置：文本
- 文档长文本：文本
- 图片数据：文本

#### 关系
- 文档类型：多对一 -> 文档类型，外键 文档类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：文档属性（聚合根）

- 表名：document_attributes

### Schema（数据模型）

#### 字段
- 属性名称：文本（必填、主键）
- 属性值：文本
- 属性说明：文本

#### 关系
- 文档：多对一 -> 文档，外键 文档编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 文档编号 设为 文档编号

## 实体：文档类型（聚合根）

- 表名：document_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 文档类型编号：文本
- 表：布尔
- 说明：文本

#### 关系
- 上级文档类型：多对一 -> 文档类型，外键 上级类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：文档类型属性（聚合根）

- 表名：document_type_attrs

### Schema（数据模型）

#### 字段
- 属性名称：文本（必填、主键）
- 说明：文本

#### 关系
- 文档类型：多对一 -> 文档类型，外键 文档类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 文档类型编号 设为 文档类型编号

## 实体：网站偏好类型（聚合根）

- 表名：web_preference_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 网页偏好类型编号：文本
- 说明：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：网站用户偏好（聚合根）

- 表名：web_user_preferences

### Schema（数据模型）

#### 字段
- 用户登录编号：文本（必填、主键）
- 参与方编号：文本（必填、主键）
- 访问编号：文本（必填、主键），说明 能够为当前会话中未登录的用户保存偏好设置
- 网页偏好值：文本

#### 关系
- 网页偏好类型：多对一 -> 网站偏好类型，外键 网页偏好类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 用户登录编号 设为 用户登录编号

## 实体：调研（聚合根）

- 表名：surveys

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 调研编号：文本
- 调研名称：文本
- 说明：文本
- 评论：文本
- 提交标题：文本
- 响应服务：文本
- 匿名：布尔，说明 允许不登录就响应调研?
- 允许多项：布尔，说明 允许对此调研进行多次响应（如果为 Y），或仅单个答案（如果为 N）?
- 允许更新：布尔，说明 允许更改响应?
- 指向具有 AcroForm 的 PDF（acro_form_content_id）：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：调研应用类型（聚合根）

- 表名：survey_appl_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 调研应用类型编号：文本
- 说明：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：调研多项响应（聚合根）

- 表名：survey_multi_resps

### Schema（数据模型）

#### 字段
- 调研多响应编号：文本（必填、主键）
- 多响应标题：文本

#### 关系
- 调研：多对一 -> 调研，外键 调研编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 调研编号 设为 调研编号

## 实体：调研多项响应列（聚合根）

- 表名：survey_multi_resp_columns

### Schema（数据模型）

#### 字段
- 调研编号：文本（必填、主键）
- 调研多响应编号：文本（必填、主键）
- 调研多响应列编号：文本（必填、主键）
- 列标题：文本
- 序列编号：整数

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 调研编号 设为 调研编号

## 实体：调研页面（聚合根）

- 表名：survey_pages

### Schema（数据模型）

#### 字段
- 调研页面序列编号：文本（必填、主键）
- 页面名称：文本
- 序列编号：整数

#### 关系
- 调研：多对一 -> 调研，外键 调研编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 调研编号 设为 调研编号

## 实体：调研问题（聚合根）

- 表名：survey_questions

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 调研问题编号：文本
- 说明：文本
- 问题：文本
- 提示：文本
- 枚举类型编号：文本
- 地理编号：文本
- 格式文本：文本

#### 关系
- 调研问题类型：多对一 -> 调研问题类型，外键 调研问题类型编号
- 调研问题分类：多对一 -> 调研问题分类，外键 调研问题分类编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：调研问题应用（聚合根）

- 表名：survey_question_appls

### Schema（数据模型）

#### 字段
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间
- 调研页面序列编号：文本
- 调研多响应编号：文本
- 调研多响应列编号：文本，说明 用于可选地将此问题关联到多响应集中的特定列；这样可以将单个问题关联到问题/列网格中的每个单元格；这对 AcroForm 往返很有用，其中目标 PDF 需要与每个单元格关联一个问题，或甚至同一问题应用不同的 externalFieldRef 值
- 必填字段：布尔
- 序列编号：整数
- 外部字段参考：文本，说明 外部字段 ID/参考；用于 AcroForms 跟踪字段 ID
- 调研问题编号：文本，说明 这两个 with* 字段用于指定此问题仅在已选择该选项时出现
- 调研选项序列编号：文本

#### 关系
- 调研：多对一 -> 调研，外键 调研编号
- 调研问题：多对一 -> 调研问题，外键 调研问题编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 调研编号 设为 调研编号

## 实体：调研问题分类（聚合根）

- 表名：survey_question_categories

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 调研问题分类编号：文本
- 说明：文本

#### 关系
- 上级调研问题分类：多对一 -> 调研问题分类，外键 上级分类编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：调研问题选项（聚合根）

- 表名：survey_question_options

### Schema（数据模型）

#### 字段
- 调研选项序列编号：文本（必填、主键）
- 说明：文本
- 序列编号：整数
- 金额基础：金额
- 金额基础单位编号：文本
- 重量系数：浮点
- 时长：整数
- 时长单位编号：文本

#### 关系
- 调研问题：多对一 -> 调研问题，外键 调研问题编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 调研问题编号 设为 调研问题编号

## 实体：调研问题类型（聚合根）

- 表名：survey_question_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 调研问题类型编号：文本
- 说明：文本

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：调研响应（聚合根）

- 表名：survey_responses

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 调研响应编号：文本
- 参与方编号：文本
- 响应日期：日期时间
- 最近修改日期：日期时间
- 参考号编号：文本
- 通用反馈：文本
- 订单编号：文本
- 订单项序列编号：文本
- 状态编号：文本

#### 关系
- 调研：多对一 -> 调研，外键 调研编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：调研响应回答（聚合根）

- 表名：survey_response_answers

### Schema（数据模型）

#### 字段
- 调研多响应列编号：文本（必填、主键），说明 需要这个来支持不同多响应列的多个响应；如果不属于多响应，则为 _NA_
- 调研多响应编号：文本，说明 这不是主键的一部分，但应填充以便可以更轻松地查找 SurveyMultiRespColumn
- 布尔响应：布尔
- 币种响应：金额
- 浮点响应：浮点
- 数值响应：整数
- 长文本响应：文本
- 调研选项序列编号：文本
- 已回答日期：日期时间
- 金额基础：金额
- 金额基础单位编号：文本
- 重量系数：浮点
- 时长：整数
- 时长单位编号：文本
- 序列编号：整数

#### 关系
- 调研响应：多对一 -> 调研响应，外键 调研响应编号
- 调研问题：多对一 -> 调研问题，外键 调研问题编号
- 内容：多对一 -> 内容，外键 内容编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 调研响应编号 设为 调研响应编号

## 实体：调研触发（聚合根）

- 表名：survey_triggers

### Schema（数据模型）

#### 字段
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间

#### 关系
- 调研：多对一 -> 调研，外键 调研编号
- 调研应用类型：多对一 -> 调研应用类型，外键 调研应用类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 调研编号 设为 调研编号

## 实体：网页站点内容（聚合根）

- 表名：web_site_contents

### Schema（数据模型）

#### 字段
- 网页站点编号：文本（必填、主键）
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间

#### 关系
- 内容：多对一 -> 内容，外键 内容编号
- 网页站点内容类型：多对一 -> 网页站点内容类型，外键 网页站点内容类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 网页站点编号 设为 网页站点编号

## 实体：网页站点内容类型（聚合根）

- 表名：web_site_content_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 网页站点内容类型编号：文本
- 说明：文本
- 表：布尔

#### 关系
- 上级网页站点内容类型：多对一 -> 网页站点内容类型，外键 上级类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：网页站点路径别名（聚合根）

- 表名：web_site_path_aliases

### Schema（数据模型）

#### 字段
- 网页站点编号：文本（必填、主键）
- 路径别名：文本（必填、主键）
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间
- 别名：文本
- 映射键：文本

#### 关系
- 内容：多对一 -> 内容，外键 内容编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 网页站点编号 设为 网页站点编号

## 实体：网页站点发布点（聚合根）

- 表名：web_site_publish_points

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 模板标题：文本
- 样式表文件：文本
- 标识：文本
- 徽标标识：文本
- 行标识：文本
- 左条编号：文本
- 右侧条编号：文本
- 内容部门：文本
- 关于内容编号：文本

#### 关系
- 内容：多对一 -> 内容，外键 内容编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：网页站点角色（聚合根）

- 表名：web_site_roles

### Schema（数据模型）

#### 字段
- 参与方编号：文本（必填、主键）
- 角色类型编号：文本（必填、主键）
- 网页站点编号：文本（必填、主键）
- 来源日期：日期时间（必填、主键）
- 至日期：日期时间
- 序列编号：整数

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 参与方编号 设为 参与方编号

## 实体：内容关键词（聚合根）

- 表名：content_keywords

### Schema（数据模型）

#### 字段
- 关键词：文本（必填、主键）
- 相关度重量：整数

#### 关系
- 内容：多对一 -> 内容，外键 内容编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 内容编号 设为 内容编号

## 实体：内容搜索约束（聚合根）

- 表名：content_search_constraints

### Schema（数据模型）

#### 字段
- 约束序列编号：文本（必填、主键）
- 约束名称：文本
- 信息文本：文本
- 包含子分类：布尔
- 且：布尔
- 任意前缀：布尔
- 任意后缀：布尔
- 移除词干：布尔
- 低值：文本
- 高值：文本

#### 关系
- 内容搜索结果：多对一 -> 内容搜索结果，外键 内容搜索结果编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 内容搜索结果编号 设为 内容搜索结果编号

## 实体：内容搜索结果（聚合根）

- 表名：content_search_results

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 内容搜索结果编号：文本
- 访问编号：文本
- 订单名称：文本
- 升序：布尔
- 编号结果：整数
- 秒合计：浮点
- 搜索日期：日期时间

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

## 实体：网页分析配置（聚合根）

- 表名：web_analytics_configs

### Schema（数据模型）

#### 字段
- 网页站点编号：文本（必填、主键）
- 网页分析编码：文本，说明 在此处复制分析 javascript 代码，不包括开始和结束的 <script> 标签

#### 关系
- 网页分析类型：多对一 -> 网页分析类型，外键 网页分析类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 网页站点编号 设为 网页站点编号

## 实体：网页分析类型（聚合根）

- 表名：web_analytics_types

### Schema（数据模型）

#### 字段
- 编号：文本（主键、自动生成）
- 网页分析类型编号：文本
- 表：布尔
- 说明：文本

#### 关系
- 上级网页分析类型：多对一 -> 网页分析类型，外键 上级类型编号

### Conduct（行为声明）

#### 操作
- 查询
- 创建
- 更新
- 删除

#### 变更
- 在 创建 / 更新 / 删除 时，将 编号 设为 编号

