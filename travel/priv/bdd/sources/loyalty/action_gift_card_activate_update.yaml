module: LOYALTY_GIFT_CARD
contract: GiftCard.activate action contract
edge_class: action_contract
source_file: /tmp/unibo_models_ubo_only_989/loyalty/loyalty.ubo.yaml
source_summary: GIVEN 实体 GiftCard 的动作 activate 可执行，accept=[[]]，args=[owner_id:uuid!, from_date:datetime] / WHEN 执行动作 activate / THEN 输入应满足校验规则，状态变更与副作用应生效，应触发关联事件，应符合工作流步骤约束 / 补充场景：create -> activate -> topup(200) -> charge(150) -> charge(50) -> status=depleted；create(reloadable=false) -> activate -> topup 应报错；create -> activate -> cancel_card
