module: CRM_LEAD_STAGE
contract: LeadStage.create action contract
edge_class: action_contract
source_file: /tmp/unibo_models_ubo_only_989/crm/crm.ubo.yaml
source_summary: GIVEN 实体 LeadStage 的动作 create 可执行，accept=[name, sequence, is_won, fold, probability, requirements, description]，args=[team_id:uuid] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create 新建阶段并设置 sequence，update 修改名称和概率；create 团队专属阶段（team_id 非空），仅该团队可见；create is_won=true 的赢单阶段
