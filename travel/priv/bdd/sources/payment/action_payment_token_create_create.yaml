module: PAYMENT_PAYMENT_TOKEN
contract: PaymentToken.create action contract
edge_class: action_contract
source_file: /tmp/unibo_models_ubo_only_989/payment/payment.ubo.yaml
source_summary: GIVEN 实体 PaymentToken 的动作 create 可执行，accept=[party_id, provider_id, token_reference, card_last_four, card_brand, expiry_date, is_default]，args=[无显式参数] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create 创建令牌 -> update 设为默认 -> revoke 吊销；create -> expire 标记过期
