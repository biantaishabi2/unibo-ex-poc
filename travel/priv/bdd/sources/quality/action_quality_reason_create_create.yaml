module: QUALITY_QUALITY_REASON
contract: QualityReason.create action contract
edge_class: action_contract
source_file: /tmp/unibo_models_ubo_only_989/quality/quality.ubo.yaml
source_summary: GIVEN 实体 QualityReason 的动作 create 可执行，accept=[name, description]，args=[无显式参数] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create 创建根因 -> update 修改根因描述 -> destroy 删除根因
