module: STUDIO_AUTOMATION_RULE
contract: AutomationRule.create action contract
edge_class: action_contract
source_file: /tmp/unibo_models_ubo_only_989/studio/studio.ubo.yaml
source_summary: GIVEN 实体 AutomationRule 的动作 create 可执行，accept=[name, trigger_type, trigger_config, condition_domain, action_type, action_config, sequence, is_active]，args=[model_id:bigint!] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create 创建自动化规则 -> update 修改规则配置 -> execute 执行规则；create 创建自动化规则 -> destroy 删除规则
