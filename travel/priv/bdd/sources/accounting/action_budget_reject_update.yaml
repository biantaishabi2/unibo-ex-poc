module: ACCOUNTING_BUDGET
contract: Budget.reject action contract
edge_class: action_contract
source_file: /tmp/unibo_models_ubo_only_989/accounting/accounting.ubo.yaml
source_summary: GIVEN 实体 Budget 的动作 reject 可执行，accept=[(未声明)]，args=[reason:string!] / WHEN 执行动作 reject / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：submit 后 reject 驳回，可修改后再次 submit
