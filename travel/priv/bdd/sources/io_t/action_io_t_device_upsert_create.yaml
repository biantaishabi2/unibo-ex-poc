module: IO_T_IO_T_DEVICE
contract: IoTDevice.upsert action contract
edge_class: action_contract
source_file: /tmp/unibo_models_ubo_only_989/iot/iot.ubo.yaml
source_summary: GIVEN 实体 IoTDevice 的动作 upsert 可执行，accept=[iot_box_id, name, identifier, device_type, connection, driver_name, capabilities, configuration, manufacturer, model]，args=[无显式参数] / WHEN 执行动作 upsert / THEN 输入应满足校验规则，状态变更与副作用应生效，应触发关联事件，应符合工作流步骤约束 / 补充场景：事件触发规则匹配；每个设备事件都写日志；POS 订单验证后调用；制造工单步骤；质检检查；仓库操作
