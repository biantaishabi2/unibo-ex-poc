module: IO_T_TRIGGER_RULE
contract: TriggerRule.create action contract
edge_class: action_contract
source_file: /tmp/unibo_models_ubo_only_989/iot/iot.ubo.yaml
source_summary: GIVEN 实体 TriggerRule 的动作 create 可执行，accept=[name, description, device_id, event_type, condition, action_type, action_config, is_active, priority, stop_on_match, cooldown_seconds, error_action]，args=[无显式参数] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应触发关联事件，应符合工作流步骤约束 / 补充场景：print_report 动作渲染并发送打印指令；create_record / update_field 创建或更新任意模型记录；call_webhook 调用外部 webhook；notify_user 推送通知给指定用户；print_report 向 box 发送打印指令
