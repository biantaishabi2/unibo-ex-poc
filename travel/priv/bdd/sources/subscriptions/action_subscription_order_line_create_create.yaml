module: SUBSCRIPTIONS_SUBSCRIPTION_ORDER_LINE
contract: SubscriptionOrderLine.create action contract
edge_class: action_contract
source_file: /tmp/unibo_models_ubo_only_989/subscriptions/subscriptions.ubo.yaml
source_summary: GIVEN 实体 SubscriptionOrderLine 的动作 create 可执行，accept=[name, quantity, price_unit, discount, recurring]，args=[product_id:uuid!] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create 创建周期性订阅行，update 修改数量和折扣；create 创建一次性订阅行（recurring=false）；create 数量为0时校验失败；create 单价为负时校验失败；create 折扣超过100%时校验失败
