module: POS_RESTAURANT_TABLE
contract: RestaurantTable.create action contract
edge_class: action_contract
source_file: /tmp/unibo_models_ubo_only_989/pos/pos.ubo.yaml
source_summary: GIVEN 实体 RestaurantTable 的动作 create 可执行，accept=[name, shape, seats, position_h, position_v, width, height, color, active]，args=[floor_id:uuid!] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create 新建餐桌，update 修改座位数和位置；create 新建餐桌，destroy 删除（无活跃会话）；create 座位数为0时校验失败
