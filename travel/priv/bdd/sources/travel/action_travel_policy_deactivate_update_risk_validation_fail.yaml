module: TRAVEL_TRAVEL_POLICY
contract: TravelPolicy.deactivate action contract (risk:validation_fail)
edge_class: action_contract
source_file: models/travel/travel.ubo.yaml
source_summary: GIVEN 实体 TravelPolicy 的动作 deactivate 输入非法 / WHEN 执行动作 deactivate / THEN 应返回校验失败且不产生持久化副作用
expected_facts:
- validation_triggered
