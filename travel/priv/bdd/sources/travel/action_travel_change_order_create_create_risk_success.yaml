module: TRAVEL_TRAVEL_CHANGE_ORDER
contract: TravelChangeOrder.create action contract (risk:success)
edge_class: action_contract
source_file: models/travel/travel.ubo.yaml
source_summary: GIVEN 实体 TravelChangeOrder 的动作 create 可执行 / WHEN 执行动作 create / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create 创建改签单 -> reject 审批拒绝
expected_facts:
- record_persisted
