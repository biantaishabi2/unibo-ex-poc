module: TRAVEL_TRAVEL_ORDER
contract: TravelOrder.approve_cancel action contract (risk:success)
edge_class: action_contract
source_file: models/travel/travel.ubo.yaml
source_summary: GIVEN 实体 TravelOrder 的动作 approve_cancel 可执行 / WHEN 执行动作 approve_cancel / THEN 成功路径应满足业务约束并持久化结果 / 补充场景：create_order 创建订单 -> confirm_quote 确认报价 -> submit_order 提交订单 -> mark_payment_succeeded 支付成功 -> mark_booked 预订成功 -> request_cancel 发起取消 -> approve_cancel 取消成功
expected_facts:
- record_persisted
- state_change
precondition_actions:
- request_cancel
