module: TRAVEL_TRAVEL_ORDER
contract: TravelOrder.cancel_waitlist action contract (risk:change_effect)
edge_class: action_contract
source_file: models/travel/travel.ubo.yaml
source_summary: GIVEN 实体 TravelOrder 的动作 cancel_waitlist 触发副作用 / WHEN 执行动作 cancel_waitlist / THEN 变更副作用应完整生效且状态一致 / 补充场景：create_order 创建 train 订单 -> confirm_quote 确认报价 -> submit_waitlist 提交候补 -> mark_payment_succeeded 支付成功 -> cancel_waitlist 取消候补
expected_facts:
- record_persisted
- state_change
- change_effect
precondition_actions:
- mark_payment_succeeded
- submit_waitlist
