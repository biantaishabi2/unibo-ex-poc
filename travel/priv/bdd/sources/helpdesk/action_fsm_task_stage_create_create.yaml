module: HELPDESK_FSM_TASK_STAGE
contract: FsmTaskStage.create action contract
edge_class: action_contract
source_file: /tmp/unibo_models_ubo_only_989/helpdesk/helpdesk.ubo.yaml
source_summary: GIVEN 实体 FsmTaskStage 的动作 create 可执行，accept=[name, sequence, fold]，args=[无显式参数] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效，应符合工作流步骤约束 / 补充场景：create 新建阶段，update 修改排序和折叠状态；create 新建阶段，update 设置 fold=true 标记为关闭阶段
