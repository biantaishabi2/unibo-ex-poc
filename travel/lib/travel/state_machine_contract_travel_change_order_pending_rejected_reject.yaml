module: TRAVEL_TRAVEL_CHANGE_ORDER
contract: TravelChangeOrder.state_machine.pending->rejected.reject
edge_class: state_machine_contract
source_file: /home/wangbo/unibo_ex_poc-travel-recompile/travel/models/travel.ubo.yaml
source_summary: state transition pending → rejected via reject should succeed
expected_facts:
- state_transition_verified
entity: TravelChangeOrder
domain: Travel
transition:
  from: pending
  to: rejected
  event: reject
  expected: success
state_attribute: status
