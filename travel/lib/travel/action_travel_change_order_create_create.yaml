module: TRAVEL_TRAVEL_CHANGE_ORDER
contract: TravelChangeOrder.create action contract
edge_class: action_contract
source_file: /home/wangbo/unibo_ex_poc-travel-recompile/travel/models/travel.ubo.yaml
source_summary: GIVEN 实体 TravelChangeOrder 的动作 create 可执行，accept=[original_order_id, change_reason, price_difference, change_fee, new_offer_id, approval_mode]，args=[无显式参数] / WHEN 执行动作 create / THEN 输入应满足校验规则，状态变更与副作用应生效 / 补充场景：create 创建改签单 -> reject 审批拒绝
expected_facts:
- action_executed
