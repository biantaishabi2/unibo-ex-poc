module: TRAVEL_FLIGHT_OFFER
contract: FlightOffer.expire action contract (risk:validation_fail)
edge_class: action_contract
source_file: /home/wangbo/unibo_ex_poc-travel-recompile/travel/models/travel.ubo.yaml
source_summary: GIVEN 实体 FlightOffer 的动作 expire 输入非法 / WHEN 执行动作 expire / THEN 应返回校验失败且不产生持久化副作用
expected_facts:
- validation_triggered
precondition_actions:
- activate
