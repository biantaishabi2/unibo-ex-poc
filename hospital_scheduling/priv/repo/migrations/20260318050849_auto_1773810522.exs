defmodule HospitalScheduling.Repo.Migrations.Auto1773810522 do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    # 注意: 这些列从 jsonb 类型变更为 text[]（PostgreSQL 原生数组）
    # 由于列是空的或测试数据，直接删除旧列并添加新列
    alter table(:scheduling_shift_preferences) do
      remove :unavailable_dates
      remove :preferred_shift_tags
    end
    alter table(:scheduling_shift_preferences) do
      add :unavailable_dates, {:array, :text}, default: []
      add :preferred_shift_tags, {:array, :text}, default: []
    end

    alter table(:scheduling_medical_staff_profiles) do
      remove :work_restrictions
    end
    alter table(:scheduling_medical_staff_profiles) do
      add :work_restrictions, {:array, :text}, default: []
    end

    alter table(:scheduling_coverage_requirements) do
      remove :required_skill_tags
    end
    alter table(:scheduling_coverage_requirements) do
      add :required_skill_tags, {:array, :text}, default: []
    end
  end

  def down do
    alter table(:scheduling_coverage_requirements) do
      remove :required_skill_tags
    end
    alter table(:scheduling_coverage_requirements) do
      add :required_skill_tags, :map
    end

    alter table(:scheduling_medical_staff_profiles) do
      remove :work_restrictions
    end
    alter table(:scheduling_medical_staff_profiles) do
      add :work_restrictions, :map
    end

    alter table(:scheduling_shift_preferences) do
      remove :preferred_shift_tags
      remove :unavailable_dates
    end
    alter table(:scheduling_shift_preferences) do
      add :preferred_shift_tags, :map
      add :unavailable_dates, :map
    end
  end
end
