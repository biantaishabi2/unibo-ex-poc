defmodule HospitalScheduling.Repo.Migrations.AddVersionsUserId do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:users, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
    end

    alter table(:scheduling_solver_runs_versions) do
      add :user_id,
          references(:users,
            column: :id,
            name: "scheduling_solver_runs_versions_user_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :nothing,
            on_update: :update_all
          )
    end

    alter table(:scheduling_shift_types_versions) do
      add :user_id,
          references(:users,
            column: :id,
            name: "scheduling_shift_types_versions_user_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :nothing,
            on_update: :update_all
          )
    end

    alter table(:scheduling_shift_preferences_versions) do
      add :user_id,
          references(:users,
            column: :id,
            name: "scheduling_shift_preferences_versions_user_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :nothing,
            on_update: :update_all
          )
    end

    alter table(:scheduling_shift_assignments_versions) do
      add :user_id,
          references(:users,
            column: :id,
            name: "scheduling_shift_assignments_versions_user_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :nothing,
            on_update: :update_all
          )
    end

    alter table(:scheduling_schedule_versions_versions) do
      add :user_id,
          references(:users,
            column: :id,
            name: "scheduling_schedule_versions_versions_user_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :nothing,
            on_update: :update_all
          )
    end

    alter table(:scheduling_periods_versions) do
      add :user_id,
          references(:users,
            column: :id,
            name: "scheduling_periods_versions_user_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :nothing,
            on_update: :update_all
          )
    end

    alter table(:scheduling_medical_staff_profiles_versions) do
      add :user_id,
          references(:users,
            column: :id,
            name: "scheduling_medical_staff_profiles_versions_user_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :nothing,
            on_update: :update_all
          )
    end

    alter table(:scheduling_coverage_requirements_versions) do
      add :user_id,
          references(:users,
            column: :id,
            name: "scheduling_coverage_requirements_versions_user_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :nothing,
            on_update: :update_all
          )
    end

    alter table(:scheduling_constraints_versions) do
      add :user_id,
          references(:users,
            column: :id,
            name: "scheduling_constraints_versions_user_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :nothing,
            on_update: :update_all
          )
    end

    alter table(:scheduling_constraint_violations_versions) do
      add :user_id,
          references(:users,
            column: :id,
            name: "scheduling_constraint_violations_versions_user_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :nothing,
            on_update: :update_all
          )
    end
  end

  def down do
    drop constraint(
           :scheduling_constraint_violations_versions,
           "scheduling_constraint_violations_versions_user_id_fkey"
         )

    alter table(:scheduling_constraint_violations_versions) do
      remove :user_id
    end

    drop constraint(
           :scheduling_constraints_versions,
           "scheduling_constraints_versions_user_id_fkey"
         )

    alter table(:scheduling_constraints_versions) do
      remove :user_id
    end

    drop constraint(
           :scheduling_coverage_requirements_versions,
           "scheduling_coverage_requirements_versions_user_id_fkey"
         )

    alter table(:scheduling_coverage_requirements_versions) do
      remove :user_id
    end

    drop constraint(
           :scheduling_medical_staff_profiles_versions,
           "scheduling_medical_staff_profiles_versions_user_id_fkey"
         )

    alter table(:scheduling_medical_staff_profiles_versions) do
      remove :user_id
    end

    drop constraint(:scheduling_periods_versions, "scheduling_periods_versions_user_id_fkey")

    alter table(:scheduling_periods_versions) do
      remove :user_id
    end

    drop constraint(
           :scheduling_schedule_versions_versions,
           "scheduling_schedule_versions_versions_user_id_fkey"
         )

    alter table(:scheduling_schedule_versions_versions) do
      remove :user_id
    end

    drop constraint(
           :scheduling_shift_assignments_versions,
           "scheduling_shift_assignments_versions_user_id_fkey"
         )

    alter table(:scheduling_shift_assignments_versions) do
      remove :user_id
    end

    drop constraint(
           :scheduling_shift_preferences_versions,
           "scheduling_shift_preferences_versions_user_id_fkey"
         )

    alter table(:scheduling_shift_preferences_versions) do
      remove :user_id
    end

    drop constraint(
           :scheduling_shift_types_versions,
           "scheduling_shift_types_versions_user_id_fkey"
         )

    alter table(:scheduling_shift_types_versions) do
      remove :user_id
    end

    drop constraint(
           :scheduling_solver_runs_versions,
           "scheduling_solver_runs_versions_user_id_fkey"
         )

    alter table(:scheduling_solver_runs_versions) do
      remove :user_id
    end

    drop table(:users)
  end
end
