module: SCHEDULING_SCHEDULING_CONSTRAINT
contract: SchedulingConstraint.create action contract
edge_class: action_contract
source_file: models/scheduling/scheduling.ubo.yaml
source_summary: GIVEN 实体 SchedulingConstraint 的动作 create 可执行，accept=[name, constraint_type, category, params, weight, enabled, notes]，args=[无显式参数] / WHEN 执行动作 create / THEN 输入应满足校验规则
expected_facts:
- action_executed
