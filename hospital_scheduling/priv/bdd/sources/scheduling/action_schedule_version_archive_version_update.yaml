module: SCHEDULING_SCHEDULE_VERSION
contract: ScheduleVersion.archive_version action contract
edge_class: action_contract
source_file: models/scheduling/scheduling.ubo.yaml
source_summary: GIVEN 实体 ScheduleVersion 的动作 archive_version 可执行，accept=[[]]，args=[无显式参数] / WHEN 执行动作 archive_version / THEN 输入应满足校验规则
expected_facts:
- action_executed
